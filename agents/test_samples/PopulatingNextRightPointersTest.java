import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PopulatingNextRightPointersTest {
    @Test
    void testConnect() {
        assertNotNull(PopulatingNextRightPointers.connect(null));
    }
}