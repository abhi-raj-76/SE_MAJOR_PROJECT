import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SymmetricTreeIITest {
    @Test
    void testSymmetric() {
        assertFalse(SymmetricTreeII.isSymmetric(null));
    }
}