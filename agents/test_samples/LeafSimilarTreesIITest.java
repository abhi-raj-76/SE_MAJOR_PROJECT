import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class LeafSimilarTreesIITest {
    @Test
    void testLeafSimilar() {
        assertTrue(LeafSimilarTreesII.leafSimilar(null, null));
    }
}