import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PopulatingNextRightPointersIITest {
    @Test
    void testConnect() {
        assertNotNull(PopulatingNextRightPointersII.connect(null));
    }
}