import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class LongestUnivaluePathTest {
    @Test
    void testLongest() {
        assertEquals(0, LongestUnivaluePath.longestUnivaluePath(null));
    }
}