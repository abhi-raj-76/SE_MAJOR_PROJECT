import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class LastStoneWeightIVTest {
    @Test
    void testLastStone() {
        assertEquals(0, LastStoneWeightIV.lastStoneWeightII(new int[]{}));
    }
}