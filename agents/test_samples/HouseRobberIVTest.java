import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class HouseRobberIVTest {
    @Test
    void testRob() {
        assertEquals(0, HouseRobberIV.rob(new int[]{}));
    }
}