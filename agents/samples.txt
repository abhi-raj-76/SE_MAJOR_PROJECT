### SAMPLE 15
// Class: VowelFrequencyAnalyzer
// Status: CORRECT
// File: VowelFrequencyAnalyzer.java
public class VowelFrequencyAnalyzer {
    public static int countVowels(String text) {
        if (text == null) {
            return 0;
        }

        int count = 0;

        for (char ch : text.toLowerCase().toCharArray()) {
            if (ch == 'a' ||
                ch == 'e' ||
                ch == 'i' ||
                ch == 'o' ||
                ch == 'u') {

                count++;
            }
        }

        return count;
    }
}

// File: VowelFrequencyAnalyzerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class VowelFrequencyAnalyzerTest {
    @Test
    void testWords() {
        assertEquals(
                5,
                VowelFrequencyAnalyzer.countVowels("Education")
        );

        assertEquals(
                2,
                VowelFrequencyAnalyzer.countVowels("Java")
        );
    }

    @Test
    void testEmpty() {
        assertEquals(
                0,
                VowelFrequencyAnalyzer.countVowels("")
        );
    }

    @Test
    void testNull() {
        assertEquals(
                0,
                VowelFrequencyAnalyzer.countVowels(null)
        );
    }
}

### SAMPLE 16
// Class: IncorrectLeapYearChecker
// Status: BUGGY
// File: IncorrectLeapYearChecker.java
public class IncorrectLeapYearChecker {
    public static boolean isLeap(int year) {
        return year % 4 == 0; // BUG
    }
}

// File: IncorrectLeapYearCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectLeapYearCheckerTest {
    @Test
    void testLeapYears() {
        assertTrue(
                IncorrectLeapYearChecker.isLeap(2020)
        );

        assertTrue(
                IncorrectLeapYearChecker.isLeap(2000)
        );
    }

    @Test
    void testNonLeapYears() {
        assertFalse(
                IncorrectLeapYearChecker.isLeap(1900)
        );

        assertFalse(
                IncorrectLeapYearChecker.isLeap(2023)
        );
    }

    @Test
    void testEdge() {
        assertTrue(
                IncorrectLeapYearChecker.isLeap(4)
        );
    }
}

### SAMPLE 17
// Class: LinkedListReverser
// Status: CORRECT
// File: LinkedListReverser.java
public class LinkedListReverser {
    static class Node {
        int value;
        Node next;

        Node(int value) {
            this.value = value;
        }
    }

    public Node reverse(Node head) {
        Node prev = null;

        while (head != null) {
            Node next = head.next;

            head.next = prev;
            prev = head;
            head = next;
        }

        return prev;
    }
}

// File: LinkedListReverserTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class LinkedListReverserTest {
    @Test
    void testReverse() {
        LinkedListReverser list =
                new LinkedListReverser();

        LinkedListReverser.Node a =
                new LinkedListReverser.Node(1);

        LinkedListReverser.Node b =
                new LinkedListReverser.Node(2);

        LinkedListReverser.Node c =
                new LinkedListReverser.Node(3);

        a.next = b;
        b.next = c;

        LinkedListReverser.Node head =
                list.reverse(a);

        assertEquals(3, head.value);
        assertEquals(2, head.next.value);
        assertEquals(1, head.next.next.value);
    }

    @Test
    void testSingle() {
        LinkedListReverser list =
                new LinkedListReverser();

        LinkedListReverser.Node a =
                new LinkedListReverser.Node(9);

        LinkedListReverser.Node head =
                list.reverse(a);

        assertEquals(9, head.value);
    }

    @Test
    void testNull() {
        LinkedListReverser list =
                new LinkedListReverser();

        assertNull(list.reverse(null));
    }
}

### SAMPLE 18
// Class: BrokenArrayAccess
// Status: RUNTIME_ERROR
// File: BrokenArrayAccess.java
public class BrokenArrayAccess {
    public static int getLast(int[] arr) {
        return arr[arr.length];
    }
}

// File: BrokenArrayAccessTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenArrayAccessTest {
    @Test
    void testArray() {
        assertEquals(
                5,
                BrokenArrayAccess.getLast(
                        new int[]{1,2,3,4,5}
                )
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                7,
                BrokenArrayAccess.getLast(
                        new int[]{7}
                )
        );
    }

    @Test
    void testEmpty() {
        assertThrows(
                ArrayIndexOutOfBoundsException.class,
                () -> BrokenArrayAccess.getLast(
                        new int[]{}
                )
        );
    }
}

### SAMPLE 19
// Class: CharacterDuplicateRemover
// Status: CORRECT
// File: CharacterDuplicateRemover.java
import java.util.LinkedHashSet;

public class CharacterDuplicateRemover {
    public static String removeDuplicates(String text) {
        LinkedHashSet<Character> set =
                new LinkedHashSet<>();

        for (char ch : text.toCharArray()) {
            set.add(ch);
        }

        StringBuilder sb =
                new StringBuilder();

        for (char ch : set) {
            sb.append(ch);
        }

        return sb.toString();
    }
}

// File: CharacterDuplicateRemoverTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CharacterDuplicateRemoverTest {
    @Test
    void testRemove() {
        assertEquals(
                "ban",
                CharacterDuplicateRemover
                        .removeDuplicates("banana")
        );
    }

    @Test
    void testUnique() {
        assertEquals(
                "abc",
                CharacterDuplicateRemover
                        .removeDuplicates("abc")
        );
    }

    @Test
    void testEmpty() {
        assertEquals(
                "",
                CharacterDuplicateRemover
                        .removeDuplicates("")
        );
    }
}

### SAMPLE 20
// Class: MissingReturnCalculator
// Status: COMPILATION_ERROR
// File: MissingReturnCalculator.java
public class MissingReturnCalculator {
    public int multiply(int a, int b) {
        int result = a * b;
    }
}

// File: MissingReturnCalculatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingReturnCalculatorTest {
    @Test
    void testMultiply() {
        MissingReturnCalculator m =
                new MissingReturnCalculator();

        assertEquals(
                20,
                m.multiply(4,5)
        );
    }

    @Test
    void testZero() {
        MissingReturnCalculator m =
                new MissingReturnCalculator();

        assertEquals(
                0,
                m.multiply(0,10)
        );
    }

    @Test
    void testNegative() {
        MissingReturnCalculator m =
                new MissingReturnCalculator();

        assertEquals(
                -12,
                m.multiply(-3,4)
        );
    }
}
### SAMPLE 21
// Class: ArrayIntersectionFinder
// Status: CORRECT
// File: ArrayIntersectionFinder.java
import java.util.*;

public class ArrayIntersectionFinder {
    public static int[] intersection(int[] a,
                                     int[] b) {

        Set<Integer> set =
                new HashSet<>();

        for (int n : a) {
            set.add(n);
        }

        List<Integer> result =
                new ArrayList<>();

        for (int n : b) {
            if (set.contains(n)) {
                result.add(n);
                set.remove(n);
            }
        }

        int[] ans =
                new int[result.size()];

        for (int i = 0;
             i < result.size();
             i++) {

            ans[i] = result.get(i);
        }

        return ans;
    }
}

// File: ArrayIntersectionFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayIntersectionFinderTest {
    @Test
    void testIntersection() {
        assertArrayEquals(
                new int[]{2},
                ArrayIntersectionFinder.intersection(
                        new int[]{1,2,2,1},
                        new int[]{2,2}
                )
        );
    }

    @Test
    void testEmpty() {
        assertArrayEquals(
                new int[]{},
                ArrayIntersectionFinder.intersection(
                        new int[]{1,3},
                        new int[]{2,4}
                )
        );
    }

    @Test
    void testMultiple() {
        assertArrayEquals(
                new int[]{4,9},
                ArrayIntersectionFinder.intersection(
                        new int[]{4,9,5},
                        new int[]{9,4,9,8,4}
                )
        );
    }
}

### SAMPLE 22
// Class: WrongFibonacciSeries
// Status: BUGGY
// File: WrongFibonacciSeries.java
public class WrongFibonacciSeries {
    public static int fibonacci(int n) {
        if (n <= 1) {
            return n;
        }

        return fibonacci(n - 1) +
               fibonacci(n - 3); // BUG
    }
}

// File: WrongFibonacciSeriesTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class WrongFibonacciSeriesTest {
    @Test
    void testValues() {
        assertEquals(
                0,
                WrongFibonacciSeries.fibonacci(0)
        );

        assertEquals(
                1,
                WrongFibonacciSeries.fibonacci(1)
        );
    }

    @Test
    void testSequence() {
        assertEquals(
                5,
                WrongFibonacciSeries.fibonacci(5)
        );
    }

    @Test
    void testLarge() {
        assertEquals(
                13,
                WrongFibonacciSeries.fibonacci(7)
        );
    }
}

### SAMPLE 23
// Class: BalancedBracketValidator
// Status: CORRECT
// File: BalancedBracketValidator.java
import java.util.Stack;

public class BalancedBracketValidator {
    public static boolean isBalanced(String s) {
        Stack<Character> stack =
                new Stack<>();

        for (char ch : s.toCharArray()) {
            if (ch == '(' ||
                ch == '{' ||
                ch == '[') {

                stack.push(ch);
            } else {
                if (stack.isEmpty()) {
                    return false;
                }

                char top = stack.pop();

                if ((ch == ')' && top != '(') ||
                    (ch == '}' && top != '{') ||
                    (ch == ']' && top != '[')) {

                    return false;
                }
            }
        }

        return stack.isEmpty();
    }
}

// File: BalancedBracketValidatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BalancedBracketValidatorTest {
    @Test
    void testBalanced() {
        assertTrue(
                BalancedBracketValidator
                        .isBalanced("()")
        );

        assertTrue(
                BalancedBracketValidator
                        .isBalanced("{[]}")
        );
    }

    @Test
    void testInvalid() {
        assertFalse(
                BalancedBracketValidator
                        .isBalanced("(]")
        );

        assertFalse(
                BalancedBracketValidator
                        .isBalanced("([)]")
        );
    }

    @Test
    void testEmpty() {
        assertTrue(
                BalancedBracketValidator
                        .isBalanced("")
        );
    }
}

### SAMPLE 24
// Class: NullStringLengthFinder
// Status: RUNTIME_ERROR
// File: NullStringLengthFinder.java
public class NullStringLengthFinder {
    public static int length(String s) {
        return s.length();
    }
}

// File: NullStringLengthFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullStringLengthFinderTest {
    @Test
    void testLength() {
        assertEquals(
                5,
                NullStringLengthFinder.length("hello")
        );
    }

    @Test
    void testEmpty() {
        assertEquals(
                0,
                NullStringLengthFinder.length("")
        );
    }

    @Test
    void testNull() {
        assertThrows(
                NullPointerException.class,
                () -> NullStringLengthFinder.length(null)
        );
    }
}

### SAMPLE 25
// Class: MatrixTransposeGenerator
// Status: CORRECT
// File: MatrixTransposeGenerator.java
public class MatrixTransposeGenerator {
    public static int[][] transpose(int[][] matrix) {
        int rows = matrix.length;
        int cols = matrix[0].length;

        int[][] result =
                new int[cols][rows];

        for (int i = 0;
             i < rows;
             i++) {

            for (int j = 0;
                 j < cols;
                 j++) {

                result[j][i] =
                        matrix[i][j];
            }
        }

        return result;
    }
}

// File: MatrixTransposeGeneratorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MatrixTransposeGeneratorTest {
    @Test
    void testSquare() {
        int[][] result =
                MatrixTransposeGenerator
                        .transpose(
                                new int[][]{
                                        {1,2},
                                        {3,4}
                                }
                        );

        assertEquals(3, result[0][1]);
    }

    @Test
    void testRectangle() {
        int[][] result =
                MatrixTransposeGenerator
                        .transpose(
                                new int[][]{
                                        {1,2,3}
                                }
                        );

        assertEquals(3, result[2][0]);
    }

    @Test
    void testSingle() {
        int[][] result =
                MatrixTransposeGenerator
                        .transpose(
                                new int[][]{
                                        {9}
                                }
                        );

        assertEquals(9, result[0][0]);
    }
}

### SAMPLE 26
// Class: InvalidLoopBuilder
// Status: COMPILATION_ERROR
// File: InvalidLoopBuilder.java
public class InvalidLoopBuilder {
    public void print() {
        for int i = 0; i < 5; i++ {
            System.out.println(i);
        }
    }
}

// File: InvalidLoopBuilderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidLoopBuilderTest {
    @Test
    void testObject() {
        InvalidLoopBuilder i =
                new InvalidLoopBuilder();

        assertNotNull(i);
    }

    @Test
    void testAnother() {
        InvalidLoopBuilder i =
                new InvalidLoopBuilder();

        assertTrue(i != null);
    }

    @Test
    void testSimple() {
        InvalidLoopBuilder i =
                new InvalidLoopBuilder();

        assertEquals(
                InvalidLoopBuilder.class,
                i.getClass()
        );
    }
}

### SAMPLE 27
// Class: ConsecutiveCharacterCompressor
// Status: CORRECT
// File: ConsecutiveCharacterCompressor.java
public class ConsecutiveCharacterCompressor {
    public static String compress(String s) {
        if (s.isEmpty()) {
            return "";
        }

        StringBuilder sb =
                new StringBuilder();

        int count = 1;

        for (int i = 1;
             i <= s.length();
             i++) {

            if (i < s.length() &&
                s.charAt(i) ==
                s.charAt(i - 1)) {

                count++;
            } else {
                sb.append(s.charAt(i - 1));
                sb.append(count);
                count = 1;
            }
        }

        return sb.toString();
    }
}

// File: ConsecutiveCharacterCompressorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ConsecutiveCharacterCompressorTest {
    @Test
    void testCompress() {
        assertEquals(
                "a3b2",
                ConsecutiveCharacterCompressor
                        .compress("aaabb")
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                "a1",
                ConsecutiveCharacterCompressor
                        .compress("a")
        );
    }

    @Test
    void testEmpty() {
        assertEquals(
                "",
                ConsecutiveCharacterCompressor
                        .compress("")
        );
    }
}

### SAMPLE 28
// Class: DivideByZeroGenerator
// Status: RUNTIME_ERROR
// File: DivideByZeroGenerator.java
public class DivideByZeroGenerator {
    public static int divide(int a) {
        return a / 0;
    }
}

// File: DivideByZeroGeneratorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DivideByZeroGeneratorTest {
    @Test
    void testException() {
        assertThrows(
                ArithmeticException.class,
                () -> DivideByZeroGenerator.divide(5)
        );
    }

    @Test
    void testAnother() {
        assertThrows(
                ArithmeticException.class,
                () -> DivideByZeroGenerator.divide(10)
        );
    }

    @Test
    void testNegative() {
        assertThrows(
                ArithmeticException.class,
                () -> DivideByZeroGenerator.divide(-3)
        );
    }
}
### SAMPLE 29
// Class: LongestWordFinder
// Status: CORRECT
// File: LongestWordFinder.java
public class LongestWordFinder {
    public static String longestWord(String sentence) {
        String[] words =
                sentence.split(" ");

        String longest = "";

        for (String word : words) {
            if (word.length() >
                    longest.length()) {

                longest = word;
            }
        }

        return longest;
    }
}

// File: LongestWordFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class LongestWordFinderTest {
    @Test
    void testSentence() {
        assertEquals(
                "elephant",
                LongestWordFinder.longestWord(
                        "cat elephant dog"
                )
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                "java",
                LongestWordFinder.longestWord(
                        "java"
                )
        );
    }

    @Test
    void testTie() {
        assertEquals(
                "apple",
                LongestWordFinder.longestWord(
                        "apple mango"
                )
        );
    }
}

### SAMPLE 30
// Class: WrongPalindromeDetector
// Status: BUGGY
// File: WrongPalindromeDetector.java
public class WrongPalindromeDetector {
    public static boolean isPalindrome(String s) {
        String reversed =
                new StringBuilder(s)
                        .reverse()
                        .toString();

        return s == reversed; // BUG
    }
}

// File: WrongPalindromeDetectorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class WrongPalindromeDetectorTest {
    @Test
    void testPalindrome() {
        assertTrue(
                WrongPalindromeDetector
                        .isPalindrome("madam")
        );
    }

    @Test
    void testNonPalindrome() {
        assertFalse(
                WrongPalindromeDetector
                        .isPalindrome("java")
        );
    }

    @Test
    void testSingle() {
        assertTrue(
                WrongPalindromeDetector
                        .isPalindrome("a")
        );
    }
}

### SAMPLE 31
// Class: BinarySearchTreeValidator
// Status: CORRECT
// File: BinarySearchTreeValidator.java
public class BinarySearchTreeValidator {
    static class Node {
        int val;
        Node left;
        Node right;

        Node(int val) {
            this.val = val;
        }
    }

    public boolean isValid(Node root) {
        return validate(
                root,
                Long.MIN_VALUE,
                Long.MAX_VALUE
        );
    }

    private boolean validate(Node node,
                             long min,
                             long max) {

        if (node == null) {
            return true;
        }

        if (node.val <= min ||
                node.val >= max) {

            return false;
        }

        return validate(
                node.left,
                min,
                node.val
        ) &&
        validate(
                node.right,
                node.val,
                max
        );
    }
}

// File: BinarySearchTreeValidatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BinarySearchTreeValidatorTest {
    @Test
    void testValid() {
        BinarySearchTreeValidator.Node root =
                new BinarySearchTreeValidator.Node(5);

        root.left =
                new BinarySearchTreeValidator.Node(3);

        root.right =
                new BinarySearchTreeValidator.Node(8);

        BinarySearchTreeValidator b =
                new BinarySearchTreeValidator();

        assertTrue(b.isValid(root));
    }

    @Test
    void testInvalid() {
        BinarySearchTreeValidator.Node root =
                new BinarySearchTreeValidator.Node(5);

        root.left =
                new BinarySearchTreeValidator.Node(7);

        BinarySearchTreeValidator b =
                new BinarySearchTreeValidator();

        assertFalse(b.isValid(root));
    }

    @Test
    void testSingle() {
        BinarySearchTreeValidator.Node root =
                new BinarySearchTreeValidator.Node(1);

        BinarySearchTreeValidator b =
                new BinarySearchTreeValidator();

        assertTrue(b.isValid(root));
    }
}

### SAMPLE 32
// Class: InvalidArrayInitializer
// Status: COMPILATION_ERROR
// File: InvalidArrayInitializer.java
public class InvalidArrayInitializer {
    public int[] create() {
        int[] arr = new int[5
        return arr;
    }
}

// File: InvalidArrayInitializerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidArrayInitializerTest {
    @Test
    void testCreate() {
        InvalidArrayInitializer i =
                new InvalidArrayInitializer();

        assertNotNull(i);
    }

    @Test
    void testClass() {
        InvalidArrayInitializer i =
                new InvalidArrayInitializer();

        assertEquals(
                InvalidArrayInitializer.class,
                i.getClass()
        );
    }

    @Test
    void testAnother() {
        InvalidArrayInitializer i =
                new InvalidArrayInitializer();

        assertTrue(i != null);
    }
}

### SAMPLE 33
// Class: NumberDigitReverser
// Status: CORRECT
// File: NumberDigitReverser.java
public class NumberDigitReverser {
    public static int reverse(int num) {
        int result = 0;

        while (num != 0) {
            result =
                    result * 10 +
                    num % 10;

            num /= 10;
        }

        return result;
    }
}

// File: NumberDigitReverserTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NumberDigitReverserTest {
    @Test
    void testReverse() {
        assertEquals(
                321,
                NumberDigitReverser.reverse(123)
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                -321,
                NumberDigitReverser.reverse(-123)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                NumberDigitReverser.reverse(0)
        );
    }
}

### SAMPLE 34
// Class: RecursiveOverflowGenerator
// Status: RUNTIME_ERROR
// File: RecursiveOverflowGenerator.java
public class RecursiveOverflowGenerator {
    public static int recurse(int n) {
        return recurse(n + 1);
    }
}

// File: RecursiveOverflowGeneratorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class RecursiveOverflowGeneratorTest {
    @Test
    void testOverflow() {
        assertThrows(
                StackOverflowError.class,
                () -> RecursiveOverflowGenerator
                        .recurse(1)
        );
    }

    @Test
    void testAnother() {
        assertThrows(
                StackOverflowError.class,
                () -> RecursiveOverflowGenerator
                        .recurse(5)
        );
    }

    @Test
    void testNegative() {
        assertThrows(
                StackOverflowError.class,
                () -> RecursiveOverflowGenerator
                        .recurse(-1)
        );
    }
}

### SAMPLE 35
// Class: UniqueCharacterChecker
// Status: CORRECT
// File: UniqueCharacterChecker.java
import java.util.HashSet;

public class UniqueCharacterChecker {
    public static boolean hasUnique(String s) {
        HashSet<Character> set =
                new HashSet<>();

        for (char ch : s.toCharArray()) {
            if (set.contains(ch)) {
                return false;
            }

            set.add(ch);
        }

        return true;
    }
}

// File: UniqueCharacterCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class UniqueCharacterCheckerTest {
    @Test
    void testUnique() {
        assertTrue(
                UniqueCharacterChecker
                        .hasUnique("abc")
        );
    }

    @Test
    void testDuplicate() {
        assertFalse(
                UniqueCharacterChecker
                        .hasUnique("hello")
        );
    }

    @Test
    void testEmpty() {
        assertTrue(
                UniqueCharacterChecker
                        .hasUnique("")
        );
    }
}

### SAMPLE 36
// Class: IncorrectAverageCalculator
// Status: BUGGY
// File: IncorrectAverageCalculator.java
public class IncorrectAverageCalculator {
    public static double average(int[] nums) {
        int sum = 0;

        for (int n : nums) {
            sum += n;
        }

        return sum / nums.length; // BUG
    }
}

// File: IncorrectAverageCalculatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectAverageCalculatorTest {
    @Test
    void testAverage() {
        assertEquals(
                2.5,
                IncorrectAverageCalculator.average(
                        new int[]{2,3}
                ),
                0.001
        );
    }

    @Test
    void testWhole() {
        assertEquals(
                3.0,
                IncorrectAverageCalculator.average(
                        new int[]{1,2,3,6}
                ),
                0.001
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                5.0,
                IncorrectAverageCalculator.average(
                        new int[]{5}
                ),
                0.001
        );
    }
}
### SAMPLE 1621
// Class: DigitProductCalculator
// Status: CORRECT
// File: DigitProductCalculator.java
public class DigitProductCalculator {
    public static int product(int n) {
        n = Math.abs(n);

        if (n == 0) {
            return 0;
        }

        int result = 1;

        while (n > 0) {
            result *= (n % 10);
            n /= 10;
        }

        return result;
    }
}

// File: DigitProductCalculatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DigitProductCalculatorTest {
    @Test
    void testProduct() {
        assertEquals(
                24,
                DigitProductCalculator.product(234)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                DigitProductCalculator.product(0)
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                6,
                DigitProductCalculator.product(-123)
        );
    }
}

### SAMPLE 1622
// Class: BrokenPrimeCounter
// Status: BUGGY
// File: BrokenPrimeCounter.java
public class BrokenPrimeCounter {
    public static boolean isPrime(int n) {
        if (n <= 1) {
            return false;
        }

        for (int i = 2; i < n; i++) {
            if (n % i == 0) {
                return true;
            }
        }

        return false;
    }
}

// File: BrokenPrimeCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenPrimeCounterTest {
    @Test
    void testPrime() {
        assertTrue(
                BrokenPrimeCounter.isPrime(7)
        );
    }

    @Test
    void testComposite() {
        assertFalse(
                BrokenPrimeCounter.isPrime(9)
        );
    }

    @Test
    void testOne() {
        assertFalse(
                BrokenPrimeCounter.isPrime(1)
        );
    }
}

### SAMPLE 1623
// Class: BinaryOnesCounter
// Status: CORRECT
// File: BinaryOnesCounter.java
public class BinaryOnesCounter {
    public static int countOnes(int n) {
        int count = 0;

        while (n > 0) {
            count += (n & 1);
            n >>= 1;
        }

        return count;
    }
}

// File: BinaryOnesCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BinaryOnesCounterTest {
    @Test
    void testCount() {
        assertEquals(
                3,
                BinaryOnesCounter.countOnes(13)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                BinaryOnesCounter.countOnes(0)
        );
    }

    @Test
    void testSingleBit() {
        assertEquals(
                1,
                BinaryOnesCounter.countOnes(8)
        );
    }
}

### SAMPLE 1624
// Class: MissingReturnExample
// Status: COMPILATION_ERROR
// File: MissingReturnExample.java
public class MissingReturnExample {
    public int max(int a, int b) {
        if (a > b) {
            return a;
        }
    }
}

// File: MissingReturnExampleTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingReturnExampleTest {
    @Test
    void testMax() {
        MissingReturnExample m =
                new MissingReturnExample();

        assertEquals(5, m.max(5,3));
    }

    @Test
    void testSecond() {
        MissingReturnExample m =
                new MissingReturnExample();

        assertEquals(9, m.max(2,9));
    }

    @Test
    void testEqual() {
        MissingReturnExample m =
                new MissingReturnExample();

        assertEquals(4, m.max(4,4));
    }
}

### SAMPLE 1625
// Class: PalindromeSentenceChecker
// Status: CORRECT
// File: PalindromeSentenceChecker.java
public class PalindromeSentenceChecker {
    public static boolean isPalindrome(String s) {
        s = s.replaceAll("[^a-zA-Z0-9]", "")
                .toLowerCase();

        int left = 0;
        int right = s.length() - 1;

        while (left < right) {
            if (s.charAt(left) !=
                    s.charAt(right)) {
                return false;
            }

            left++;
            right--;
        }

        return true;
    }
}

// File: PalindromeSentenceCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PalindromeSentenceCheckerTest {
    @Test
    void testPalindrome() {
        assertTrue(
                PalindromeSentenceChecker.isPalindrome(
                        "A man, a plan, a canal: Panama"
                )
        );
    }

    @Test
    void testNonPalindrome() {
        assertFalse(
                PalindromeSentenceChecker.isPalindrome(
                        "hello"
                )
        );
    }

    @Test
    void testEmpty() {
        assertTrue(
                PalindromeSentenceChecker.isPalindrome("")
        );
    }
}

### SAMPLE 1626
// Class: NullArraySummer
// Status: RUNTIME_ERROR
// File: NullArraySummer.java
public class NullArraySummer {
    public int sum(int[] nums) {
        int total = 0;

        for (int n : nums) {
            total += n;
        }

        return total;
    }
}

// File: NullArraySummerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullArraySummerTest {
    @Test
    void testSum() {
        NullArraySummer n =
                new NullArraySummer();

        assertEquals(
                6,
                n.sum(new int[]{1,2,3})
        );
    }

    @Test
    void testEmpty() {
        NullArraySummer n =
                new NullArraySummer();

        assertEquals(
                0,
                n.sum(new int[]{})
        );
    }

    @Test
    void testNull() {
        NullArraySummer n =
                new NullArraySummer();

        assertThrows(
                NullPointerException.class,
                () -> n.sum(null)
        );
    }
}

### SAMPLE 1627
// Class: RecursiveDigitCounter
// Status: CORRECT
// File: RecursiveDigitCounter.java
public class RecursiveDigitCounter {
    public static int countDigits(int n) {
        n = Math.abs(n);

        if (n < 10) {
            return 1;
        }

        return 1 + countDigits(n / 10);
    }
}

// File: RecursiveDigitCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class RecursiveDigitCounterTest {
    @Test
    void testDigits() {
        assertEquals(
                5,
                RecursiveDigitCounter.countDigits(12345)
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                1,
                RecursiveDigitCounter.countDigits(7)
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                3,
                RecursiveDigitCounter.countDigits(-999)
        );
    }
}

### SAMPLE 1628
// Class: ArrayMiddleFetcher
// Status: BUGGY
// File: ArrayMiddleFetcher.java
public class ArrayMiddleFetcher {
    public static int middle(int[] nums) {
        return nums[nums.length / 2 + 1];
    }
}

// File: ArrayMiddleFetcherTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayMiddleFetcherTest {
    @Test
    void testOddLength() {
        assertEquals(
                3,
                ArrayMiddleFetcher.middle(
                        new int[]{1,2,3,4,5}
                )
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                7,
                ArrayMiddleFetcher.middle(
                        new int[]{7}
                )
        );
    }

    @Test
    void testAnother() {
        assertEquals(
                5,
                ArrayMiddleFetcher.middle(
                        new int[]{4,5,6}
                )
        );
    }
}

### SAMPLE 1629
// Class: EvenNumberCollector
// Status: CORRECT
// File: EvenNumberCollector.java
import java.util.ArrayList;
import java.util.List;

public class EvenNumberCollector {
    public static List<Integer> collect(
            int[] nums) {

        List<Integer> result =
                new ArrayList<>();

        for (int n : nums) {
            if (n % 2 == 0) {
                result.add(n);
            }
        }

        return result;
    }
}

// File: EvenNumberCollectorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.List;

class EvenNumberCollectorTest {
    @Test
    void testCollect() {
        List<Integer> result =
                EvenNumberCollector.collect(
                        new int[]{1,2,3,4,5,6}
                );

        assertEquals(3, result.size());
    }

    @Test
    void testNoEven() {
        List<Integer> result =
                EvenNumberCollector.collect(
                        new int[]{1,3,5}
                );

        assertEquals(0, result.size());
    }

    @Test
    void testEmpty() {
        List<Integer> result =
                EvenNumberCollector.collect(
                        new int[]{}
                );

        assertTrue(result.isEmpty());
    }
}

### SAMPLE 1630
// Class: DivideByZeroGenerator
// Status: RUNTIME_ERROR
// File: DivideByZeroGenerator.java
public class DivideByZeroGenerator {
    public int calculate(int a) {
        return a / 0;
    }
}

// File: DivideByZeroGeneratorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DivideByZeroGeneratorTest {
    @Test
    void testCrash() {
        DivideByZeroGenerator d =
                new DivideByZeroGenerator();

        assertThrows(
                ArithmeticException.class,
                () -> d.calculate(10)
        );
    }

    @Test
    void testAnother() {
        DivideByZeroGenerator d =
                new DivideByZeroGenerator();

        assertThrows(
                ArithmeticException.class,
                () -> d.calculate(5)
        );
    }

    @Test
    void testNegative() {
        DivideByZeroGenerator d =
                new DivideByZeroGenerator();

        assertThrows(
                ArithmeticException.class,
                () -> d.calculate(-3)
        );
    }
}
### SAMPLE 1631
// Class: VowelCounterUtility
// Status: CORRECT
// File: VowelCounterUtility.java
public class VowelCounterUtility {
    public static int countVowels(String s) {
        int count = 0;

        for (char c : s.toLowerCase().toCharArray()) {
            if ("aeiou".indexOf(c) >= 0) {
                count++;
            }
        }

        return count;
    }
}

// File: VowelCounterUtilityTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class VowelCounterUtilityTest {
    @Test
    void testVowels() {
        assertEquals(
                5,
                VowelCounterUtility.countVowels("education")
        );
    }

    @Test
    void testNoVowels() {
        assertEquals(
                0,
                VowelCounterUtility.countVowels("rhythm")
        );
    }

    @Test
    void testUpperCase() {
        assertEquals(
                2,
                VowelCounterUtility.countVowels("JAVA")
        );
    }
}

### SAMPLE 1632
// Class: IncorrectAverageFinder
// Status: BUGGY
// File: IncorrectAverageFinder.java
public class IncorrectAverageFinder {
    public static double average(int[] nums) {
        int sum = 0;

        for (int n : nums) {
            sum += n;
        }

        return sum / nums.length;
    }
}

// File: IncorrectAverageFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectAverageFinderTest {
    @Test
    void testAverage() {
        assertEquals(
                2.5,
                IncorrectAverageFinder.average(
                        new int[]{2,3}
                )
        );
    }

    @Test
    void testWholeNumber() {
        assertEquals(
                4.0,
                IncorrectAverageFinder.average(
                        new int[]{4,4,4}
                )
        );
    }

    @Test
    void testAnother() {
        assertEquals(
                3.5,
                IncorrectAverageFinder.average(
                        new int[]{3,4}
                )
        );
    }
}

### SAMPLE 1633
// Class: HexadecimalConverter
// Status: CORRECT
// File: HexadecimalConverter.java
public class HexadecimalConverter {
    public static String toHex(int n) {
        return Integer.toHexString(n)
                .toUpperCase();
    }
}

// File: HexadecimalConverterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class HexadecimalConverterTest {
    @Test
    void testHex() {
        assertEquals(
                "FF",
                HexadecimalConverter.toHex(255)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                "0",
                HexadecimalConverter.toHex(0)
        );
    }

    @Test
    void testSmall() {
        assertEquals(
                "A",
                HexadecimalConverter.toHex(10)
        );
    }
}

### SAMPLE 1634
// Class: MissingSemicolonProgram
// Status: COMPILATION_ERROR
// File: MissingSemicolonProgram.java
public class MissingSemicolonProgram {
    public int multiply(int a, int b) {
        int result = a * b
        return result;
    }
}

// File: MissingSemicolonProgramTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingSemicolonProgramTest {
    @Test
    void testMultiply() {
        MissingSemicolonProgram m =
                new MissingSemicolonProgram();

        assertEquals(12, m.multiply(3,4));
    }

    @Test
    void testZero() {
        MissingSemicolonProgram m =
                new MissingSemicolonProgram();

        assertEquals(0, m.multiply(0,5));
    }

    @Test
    void testNegative() {
        MissingSemicolonProgram m =
                new MissingSemicolonProgram();

        assertEquals(-6, m.multiply(-2,3));
    }
}

### SAMPLE 1635
// Class: UniqueCharacterChecker
// Status: CORRECT
// File: UniqueCharacterChecker.java
import java.util.HashSet;

public class UniqueCharacterChecker {
    public static boolean unique(String s) {
        HashSet<Character> set =
                new HashSet<>();

        for (char c : s.toCharArray()) {
            if (set.contains(c)) {
                return false;
            }

            set.add(c);
        }

        return true;
    }
}

// File: UniqueCharacterCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class UniqueCharacterCheckerTest {
    @Test
    void testUnique() {
        assertTrue(
                UniqueCharacterChecker.unique("abc")
        );
    }

    @Test
    void testDuplicate() {
        assertFalse(
                UniqueCharacterChecker.unique("hello")
        );
    }

    @Test
    void testEmpty() {
        assertTrue(
                UniqueCharacterChecker.unique("")
        );
    }
}

### SAMPLE 1636
// Class: NullStringLengthFinder
// Status: RUNTIME_ERROR
// File: NullStringLengthFinder.java
public class NullStringLengthFinder {
    public int length(String s) {
        return s.length();
    }
}

// File: NullStringLengthFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullStringLengthFinderTest {
    @Test
    void testLength() {
        NullStringLengthFinder n =
                new NullStringLengthFinder();

        assertEquals(5, n.length("hello"));
    }

    @Test
    void testEmpty() {
        NullStringLengthFinder n =
                new NullStringLengthFinder();

        assertEquals(0, n.length(""));
    }

    @Test
    void testNull() {
        NullStringLengthFinder n =
                new NullStringLengthFinder();

        assertThrows(
                NullPointerException.class,
                () -> n.length(null)
        );
    }
}

### SAMPLE 1637
// Class: FibonacciSequencePrinter
// Status: CORRECT
// File: FibonacciSequencePrinter.java
public class FibonacciSequencePrinter {
    public static int fibonacci(int n) {
        if (n <= 1) {
            return n;
        }

        int a = 0;
        int b = 1;

        for (int i = 2; i <= n; i++) {
            int temp = a + b;
            a = b;
            b = temp;
        }

        return b;
    }
}

// File: FibonacciSequencePrinterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class FibonacciSequencePrinterTest {
    @Test
    void testFibonacci() {
        assertEquals(
                8,
                FibonacciSequencePrinter.fibonacci(6)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                FibonacciSequencePrinter.fibonacci(0)
        );
    }

    @Test
    void testOne() {
        assertEquals(
                1,
                FibonacciSequencePrinter.fibonacci(1)
        );
    }
}

### SAMPLE 1638
// Class: WrongPalindromeValidator
// Status: BUGGY
// File: WrongPalindromeValidator.java
public class WrongPalindromeValidator {
    public static boolean check(String s) {
        String reversed =
                new StringBuilder(s)
                        .reverse()
                        .toString();

        return s == reversed;
    }
}

// File: WrongPalindromeValidatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class WrongPalindromeValidatorTest {
    @Test
    void testPalindrome() {
        assertTrue(
                WrongPalindromeValidator.check("madam")
        );
    }

    @Test
    void testNonPalindrome() {
        assertFalse(
                WrongPalindromeValidator.check("java")
        );
    }

    @Test
    void testSingle() {
        assertTrue(
                WrongPalindromeValidator.check("a")
        );
    }
}

### SAMPLE 1639
// Class: MatrixRowSumCalculator
// Status: CORRECT
// File: MatrixRowSumCalculator.java
public class MatrixRowSumCalculator {
    public static int[] rowSums(int[][] matrix) {
        int[] sums =
                new int[matrix.length];

        for (int i = 0; i < matrix.length; i++) {
            for (int j = 0;
                 j < matrix[i].length;
                 j++) {

                sums[i] += matrix[i][j];
            }
        }

        return sums;
    }
}

// File: MatrixRowSumCalculatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MatrixRowSumCalculatorTest {
    @Test
    void testRowSums() {
        int[] result =
                MatrixRowSumCalculator.rowSums(
                        new int[][]{
                                {1,2},
                                {3,4}
                        }
                );

        assertEquals(3, result[0]);
        assertEquals(7, result[1]);
    }

    @Test
    void testSingleRow() {
        int[] result =
                MatrixRowSumCalculator.rowSums(
                        new int[][]{
                                {5,5,5}
                        }
                );

        assertEquals(15, result[0]);
    }

    @Test
    void testZeroMatrix() {
        int[] result =
                MatrixRowSumCalculator.rowSums(
                        new int[][]{
                                {0,0}
                        }
                );

        assertEquals(0, result[0]);
    }
}

### SAMPLE 1640
// Class: ArrayOverflowCrash
// Status: RUNTIME_ERROR
// File: ArrayOverflowCrash.java
public class ArrayOverflowCrash {
    public int fetch(int[] nums) {
        return nums[100];
    }
}

// File: ArrayOverflowCrashTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayOverflowCrashTest {
    @Test
    void testCrash() {
        ArrayOverflowCrash a =
                new ArrayOverflowCrash();

        assertThrows(
                ArrayIndexOutOfBoundsException.class,
                () -> a.fetch(new int[]{1,2,3})
        );
    }

    @Test
    void testAnotherCrash() {
        ArrayOverflowCrash a =
                new ArrayOverflowCrash();

        assertThrows(
                ArrayIndexOutOfBoundsException.class,
                () -> a.fetch(new int[]{9})
        );
    }

    @Test
    void testEmpty() {
        ArrayOverflowCrash a =
                new ArrayOverflowCrash();

        assertThrows(
                ArrayIndexOutOfBoundsException.class,
                () -> a.fetch(new int[]{})
        );
    }
}
### SAMPLE 1641
// Class: NumberPalindromeChecker
// Status: CORRECT
// File: NumberPalindromeChecker.java
public class NumberPalindromeChecker {
    public static boolean isPalindrome(int n) {
        if (n < 0) {
            return false;
        }

        int original = n;
        int reversed = 0;

        while (n > 0) {
            reversed = reversed * 10 + n % 10;
            n /= 10;
        }

        return original == reversed;
    }
}

// File: NumberPalindromeCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NumberPalindromeCheckerTest {
    @Test
    void testPalindrome() {
        assertTrue(
                NumberPalindromeChecker.isPalindrome(121)
        );
    }

    @Test
    void testNonPalindrome() {
        assertFalse(
                NumberPalindromeChecker.isPalindrome(123)
        );
    }

    @Test
    void testNegative() {
        assertFalse(
                NumberPalindromeChecker.isPalindrome(-121)
        );
    }
}

### SAMPLE 1642
// Class: IncorrectMaxFinder
// Status: BUGGY
// File: IncorrectMaxFinder.java
public class IncorrectMaxFinder {
    public static int max(int[] nums) {
        int max = 0;

        for (int n : nums) {
            if (n > max) {
                max = n;
            }
        }

        return max;
    }
}

// File: IncorrectMaxFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectMaxFinderTest {
    @Test
    void testPositive() {
        assertEquals(
                9,
                IncorrectMaxFinder.max(
                        new int[]{1,9,3}
                )
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                -1,
                IncorrectMaxFinder.max(
                        new int[]{-5,-1,-7}
                )
        );
    }

    @Test
    void testMixed() {
        assertEquals(
                4,
                IncorrectMaxFinder.max(
                        new int[]{-1,4,2}
                )
        );
    }
}

### SAMPLE 1643
// Class: SentenceWordCounter
// Status: CORRECT
// File: SentenceWordCounter.java
public class SentenceWordCounter {
    public static int countWords(String s) {
        if (s == null ||
                s.trim().isEmpty()) {
            return 0;
        }

        return s.trim().split("\\s+").length;
    }
}

// File: SentenceWordCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SentenceWordCounterTest {
    @Test
    void testWords() {
        assertEquals(
                4,
                SentenceWordCounter.countWords(
                        "Java is very powerful"
                )
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                1,
                SentenceWordCounter.countWords("Hello")
        );
    }

    @Test
    void testEmpty() {
        assertEquals(
                0,
                SentenceWordCounter.countWords(" ")
        );
    }
}

### SAMPLE 1644
// Class: InvalidMethodStructure
// Status: COMPILATION_ERROR
// File: InvalidMethodStructure.java
public class InvalidMethodStructure {
    public int subtract(int a, int b)
        return a - b;
    }
}

// File: InvalidMethodStructureTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidMethodStructureTest {
    @Test
    void testSubtract() {
        InvalidMethodStructure i =
                new InvalidMethodStructure();

        assertEquals(2, i.subtract(5,3));
    }

    @Test
    void testNegative() {
        InvalidMethodStructure i =
                new InvalidMethodStructure();

        assertEquals(-1, i.subtract(2,3));
    }

    @Test
    void testZero() {
        InvalidMethodStructure i =
                new InvalidMethodStructure();

        assertEquals(0, i.subtract(4,4));
    }
}

### SAMPLE 1645
// Class: CharacterPositionFinder
// Status: CORRECT
// File: CharacterPositionFinder.java
public class CharacterPositionFinder {
    public static int firstIndex(
            String s,
            char target) {

        for (int i = 0; i < s.length(); i++) {
            if (s.charAt(i) == target) {
                return i;
            }
        }

        return -1;
    }
}

// File: CharacterPositionFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CharacterPositionFinderTest {
    @Test
    void testFound() {
        assertEquals(
                2,
                CharacterPositionFinder.firstIndex(
                        "apple",
                        'p'
                )
        );
    }

    @Test
    void testMissing() {
        assertEquals(
                -1,
                CharacterPositionFinder.firstIndex(
                        "java",
                        'z'
                )
        );
    }

    @Test
    void testFirstCharacter() {
        assertEquals(
                0,
                CharacterPositionFinder.firstIndex(
                        "hello",
                        'h'
                )
        );
    }
}

### SAMPLE 1646
// Class: NullMatrixTraversal
// Status: RUNTIME_ERROR
// File: NullMatrixTraversal.java
public class NullMatrixTraversal {
    public int total(int[][] matrix) {
        int sum = 0;

        for (int[] row : matrix) {
            for (int n : row) {
                sum += n;
            }
        }

        return sum;
    }
}

// File: NullMatrixTraversalTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullMatrixTraversalTest {
    @Test
    void testMatrix() {
        NullMatrixTraversal n =
                new NullMatrixTraversal();

        assertEquals(
                10,
                n.total(
                        new int[][]{
                                {1,2},
                                {3,4}
                        }
                )
        );
    }

    @Test
    void testEmpty() {
        NullMatrixTraversal n =
                new NullMatrixTraversal();

        assertEquals(
                0,
                n.total(new int[][]{})
        );
    }

    @Test
    void testNull() {
        NullMatrixTraversal n =
                new NullMatrixTraversal();

        assertThrows(
                NullPointerException.class,
                () -> n.total(null)
        );
    }
}

### SAMPLE 1647
// Class: RecursiveSumFinder
// Status: CORRECT
// File: RecursiveSumFinder.java
public class RecursiveSumFinder {
    public static int sum(int n) {
        if (n <= 0) {
            return 0;
        }

        return n + sum(n - 1);
    }
}

// File: RecursiveSumFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class RecursiveSumFinderTest {
    @Test
    void testSum() {
        assertEquals(
                15,
                RecursiveSumFinder.sum(5)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                RecursiveSumFinder.sum(0)
        );
    }

    @Test
    void testOne() {
        assertEquals(
                1,
                RecursiveSumFinder.sum(1)
        );
    }
}

### SAMPLE 1648
// Class: IncorrectEvenChecker
// Status: BUGGY
// File: IncorrectEvenChecker.java
public class IncorrectEvenChecker {
    public static boolean isEven(int n) {
        return n % 2 == 1;
    }
}

// File: IncorrectEvenCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectEvenCheckerTest {
    @Test
    void testEven() {
        assertTrue(
                IncorrectEvenChecker.isEven(4)
        );
    }

    @Test
    void testOdd() {
        assertFalse(
                IncorrectEvenChecker.isEven(5)
        );
    }

    @Test
    void testZero() {
        assertTrue(
                IncorrectEvenChecker.isEven(0)
        );
    }
}

### SAMPLE 1649
// Class: ArrayDuplicateChecker
// Status: CORRECT
// File: ArrayDuplicateChecker.java
import java.util.HashSet;

public class ArrayDuplicateChecker {
    public static boolean hasDuplicate(
            int[] nums) {

        HashSet<Integer> set =
                new HashSet<>();

        for (int n : nums) {
            if (set.contains(n)) {
                return true;
            }

            set.add(n);
        }

        return false;
    }
}

// File: ArrayDuplicateCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayDuplicateCheckerTest {
    @Test
    void testDuplicate() {
        assertTrue(
                ArrayDuplicateChecker.hasDuplicate(
                        new int[]{1,2,3,2}
                )
        );
    }

    @Test
    void testNoDuplicate() {
        assertFalse(
                ArrayDuplicateChecker.hasDuplicate(
                        new int[]{1,2,3}
                )
        );
    }

    @Test
    void testSingle() {
        assertFalse(
                ArrayDuplicateChecker.hasDuplicate(
                        new int[]{7}
                )
        );
    }
}

### SAMPLE 1650
// Class: StringCharacterCrash
// Status: RUNTIME_ERROR
// File: StringCharacterCrash.java
public class StringCharacterCrash {
    public char get(String s) {
        return s.charAt(10);
    }
}

// File: StringCharacterCrashTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class StringCharacterCrashTest {
    @Test
    void testCrash() {
        StringCharacterCrash s =
                new StringCharacterCrash();

        assertThrows(
                StringIndexOutOfBoundsException.class,
                () -> s.get("java")
        );
    }

    @Test
    void testAnotherCrash() {
        StringCharacterCrash s =
                new StringCharacterCrash();

        assertThrows(
                StringIndexOutOfBoundsException.class,
                () -> s.get("")
        );
    }

    @Test
    void testShortString() {
        StringCharacterCrash s =
                new StringCharacterCrash();

        assertThrows(
                StringIndexOutOfBoundsException.class,
                () -> s.get("hi")
        );
    }
}
### SAMPLE 1650
// Class: ReverseDigitAccumulator
// Status: CORRECT
// File: ReverseDigitAccumulator.java
public class ReverseDigitAccumulator {
    public static int reverseNumber(int num) {
        int reversed = 0;

        while (num != 0) {
            reversed = reversed * 10 + num % 10;
            num /= 10;
        }

        return reversed;
    }
}

// File: ReverseDigitAccumulatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ReverseDigitAccumulatorTest {
    @Test
    void testReverse() {
        assertEquals(
                321,
                ReverseDigitAccumulator.reverseNumber(123)
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                -654,
                ReverseDigitAccumulator.reverseNumber(-456)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                ReverseDigitAccumulator.reverseNumber(0)
        );
    }
}

### SAMPLE 1651
// Class: BrokenLoopProcessor
// Status: BUGGY
// File: BrokenLoopProcessor.java
public class BrokenLoopProcessor {
    public static int findSum(int n) {
        int sum = 0;

        for (int i = 1; i < n; i++) { // BUG
            sum += i;
        }

        return sum;
    }
}

// File: BrokenLoopProcessorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenLoopProcessorTest {
    @Test
    void testFive() {
        assertEquals(
                15,
                BrokenLoopProcessor.findSum(5)
        );
    }

    @Test
    void testOne() {
        assertEquals(
                1,
                BrokenLoopProcessor.findSum(1)
        );
    }

    @Test
    void testThree() {
        assertEquals(
                6,
                BrokenLoopProcessor.findSum(3)
        );
    }
}

### SAMPLE 1652
// Class: MatrixColumnSummer
// Status: CORRECT
// File: MatrixColumnSummer.java
public class MatrixColumnSummer {
    public static int columnSum(
            int[][] matrix,
            int col
    ) {
        int sum = 0;

        for (int i = 0; i < matrix.length; i++) {
            sum += matrix[i][col];
        }

        return sum;
    }
}

// File: MatrixColumnSummerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MatrixColumnSummerTest {
    @Test
    void testColumn() {
        int[][] matrix = {
                {1,2,3},
                {4,5,6},
                {7,8,9}
        };

        assertEquals(
                15,
                MatrixColumnSummer.columnSum(matrix,1)
        );
    }

    @Test
    void testFirstColumn() {
        int[][] matrix = {
                {2,3},
                {4,5}
        };

        assertEquals(
                6,
                MatrixColumnSummer.columnSum(matrix,0)
        );
    }

    @Test
    void testSingle() {
        int[][] matrix = {
                {10}
        };

        assertEquals(
                10,
                MatrixColumnSummer.columnSum(matrix,0)
        );
    }
}

### SAMPLE 1653
// Class: InvalidReturnTypeHandler
// Status: COMPILATION_ERROR
// File: InvalidReturnTypeHandler.java
public class InvalidReturnTypeHandler {
    public static int message() {
        return "hello";
    }
}

// File: InvalidReturnTypeHandlerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidReturnTypeHandlerTest {
    @Test
    void testMessage() {
        assertEquals(
                "hello",
                InvalidReturnTypeHandler.message()
        );
    }

    @Test
    void testNotNull() {
        assertNotNull(
                InvalidReturnTypeHandler.message()
        );
    }

    @Test
    void testValue() {
        assertTrue(
                InvalidReturnTypeHandler.message()
                        .length() > 0
        );
    }
}
### SAMPLE 1654
// Class: EvenNumberCounter
// Status: CORRECT
// File: EvenNumberCounter.java
public class EvenNumberCounter {
    public static int countEven(int[] nums) {
        int count = 0;

        for (int n : nums) {
            if (n % 2 == 0) {
                count++;
            }
        }

        return count;
    }
}

// File: EvenNumberCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class EvenNumberCounterTest {
    @Test
    void testMixed() {
        assertEquals(
                3,
                EvenNumberCounter.countEven(
                        new int[]{1,2,4,5,6}
                )
        );
    }

    @Test
    void testNoEven() {
        assertEquals(
                0,
                EvenNumberCounter.countEven(
                        new int[]{1,3,5}
                )
        );
    }

    @Test
    void testAllEven() {
        assertEquals(
                4,
                EvenNumberCounter.countEven(
                        new int[]{2,4,6,8}
                )
        );
    }
}

### SAMPLE 1655
// Class: ArrayIndexCrasher
// Status: RUNTIME_ERROR
// File: ArrayIndexCrasher.java
public class ArrayIndexCrasher {
    public static int crash(int[] nums) {
        return nums[-1];
    }
}

// File: ArrayIndexCrasherTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayIndexCrasherTest {
    @Test
    void testCrash() {
        assertEquals(
                5,
                ArrayIndexCrasher.crash(
                        new int[]{5,6,7}
                )
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                1,
                ArrayIndexCrasher.crash(
                        new int[]{1}
                )
        );
    }

    @Test
    void testAnother() {
        assertEquals(
                9,
                ArrayIndexCrasher.crash(
                        new int[]{9,2}
                )
        );
    }
}

### SAMPLE 1656
// Class: CharacterFrequencyCounter
// Status: CORRECT
// File: CharacterFrequencyCounter.java
public class CharacterFrequencyCounter {
    public static int frequency(
            String s,
            char target
    ) {
        int count = 0;

        for (char c : s.toCharArray()) {
            if (c == target) {
                count++;
            }
        }

        return count;
    }
}

// File: CharacterFrequencyCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CharacterFrequencyCounterTest {
    @Test
    void testFrequency() {
        assertEquals(
                2,
                CharacterFrequencyCounter.frequency(
                        "banana",
                        'a'
                )
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                CharacterFrequencyCounter.frequency(
                        "apple",
                        'z'
                )
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                1,
                CharacterFrequencyCounter.frequency(
                        "java",
                        'j'
                )
        );
    }
}

### SAMPLE 1657
// Class: MissingSemicolonClass
// Status: COMPILATION_ERROR
// File: MissingSemicolonClass.java
public class MissingSemicolonClass {
    public static int add(int a, int b) {
        int sum = a + b
        return sum;
    }
}

// File: MissingSemicolonClassTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingSemicolonClassTest {
    @Test
    void testAdd() {
        assertEquals(
                5,
                MissingSemicolonClass.add(2,3)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                7,
                MissingSemicolonClass.add(7,0)
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                -2,
                MissingSemicolonClass.add(-1,-1)
        );
    }
}

### SAMPLE 1658
// Class: BinaryStringConverter
// Status: CORRECT
// File: BinaryStringConverter.java
public class BinaryStringConverter {
    public static String toBinary(int n) {
        return Integer.toBinaryString(n);
    }
}

// File: BinaryStringConverterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BinaryStringConverterTest {
    @Test
    void testBinary() {
        assertEquals(
                "1010",
                BinaryStringConverter.toBinary(10)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                "0",
                BinaryStringConverter.toBinary(0)
        );
    }

    @Test
    void testOne() {
        assertEquals(
                "1",
                BinaryStringConverter.toBinary(1)
        );
    }
}
### SAMPLE 1659
// Class: FaultyDivisionMachine
// Status: RUNTIME_ERROR
// File: FaultyDivisionMachine.java
public class FaultyDivisionMachine {
    public static int divide(int a, int b) {
        return a / b;
    }
}

// File: FaultyDivisionMachineTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class FaultyDivisionMachineTest {
    @Test
    void testDivision() {
        assertEquals(
                2,
                FaultyDivisionMachine.divide(10,5)
        );
    }

    @Test
    void testZeroDivision() {
        assertEquals(
                0,
                FaultyDivisionMachine.divide(5,0)
        );
    }

    @Test
    void testAnother() {
        assertEquals(
                4,
                FaultyDivisionMachine.divide(20,5)
        );
    }
}

### SAMPLE 1660
// Class: IncorrectAverageCalculator
// Status: BUGGY
// File: IncorrectAverageCalculator.java
public class IncorrectAverageCalculator {
    public static double average(int[] nums) {
        int sum = 0;

        for (int n : nums) {
            sum += n;
        }

        return sum;
    }
}

// File: IncorrectAverageCalculatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectAverageCalculatorTest {
    @Test
    void testAverage() {
        assertEquals(
                2.0,
                IncorrectAverageCalculator.average(
                        new int[]{1,2,3}
                )
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                5.0,
                IncorrectAverageCalculator.average(
                        new int[]{5}
                )
        );
    }

    @Test
    void testLarge() {
        assertEquals(
                10.0,
                IncorrectAverageCalculator.average(
                        new int[]{5,10,15}
                )
        );
    }
}

### SAMPLE 1661
// Class: MissingBraceUtility
// Status: COMPILATION_ERROR
// File: MissingBraceUtility.java
public class MissingBraceUtility {
    public static int square(int n) {
        return n * n;

// File: MissingBraceUtilityTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingBraceUtilityTest {
    @Test
    void testSquare() {
        assertEquals(
                25,
                MissingBraceUtility.square(5)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                MissingBraceUtility.square(0)
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                9,
                MissingBraceUtility.square(-3)
        );
    }
}

### SAMPLE 1662
// Class: StringLengthReporter
// Status: CORRECT
// File: StringLengthReporter.java
public class StringLengthReporter {
    public static int length(String s) {
        return s.length();
    }
}

// File: StringLengthReporterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class StringLengthReporterTest {
    @Test
    void testWord() {
        assertEquals(
                5,
                StringLengthReporter.length("apple")
        );
    }

    @Test
    void testEmpty() {
        assertEquals(
                0,
                StringLengthReporter.length("")
        );
    }

    @Test
    void testAnother() {
        assertEquals(
                4,
                StringLengthReporter.length("java")
        );
    }
}

### SAMPLE 1663
// Class: WrongMultiplicationEngine
// Status: BUGGY
// File: WrongMultiplicationEngine.java
public class WrongMultiplicationEngine {
    public static int multiply(int a, int b) {
        return a + b;
    }
}

// File: WrongMultiplicationEngineTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class WrongMultiplicationEngineTest {
    @Test
    void testMultiply() {
        assertEquals(
                12,
                WrongMultiplicationEngine.multiply(3,4)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                WrongMultiplicationEngine.multiply(0,5)
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                -8,
                WrongMultiplicationEngine.multiply(-2,4)
        );
    }
}

### SAMPLE 1664
// Class: NullPointerGenerator
// Status: RUNTIME_ERROR
// File: NullPointerGenerator.java
public class NullPointerGenerator {
    public static int getSize(String s) {
        return s.length();
    }
}

// File: NullPointerGeneratorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullPointerGeneratorTest {
    @Test
    void testText() {
        assertEquals(
                4,
                NullPointerGenerator.getSize("java")
        );
    }

    @Test
    void testNull() {
        assertEquals(
                0,
                NullPointerGenerator.getSize(null)
        );
    }

    @Test
    void testAnother() {
        assertEquals(
                5,
                NullPointerGenerator.getSize("apple")
        );
    }
}

### SAMPLE 1665
// Class: AscendingOrderChecker
// Status: CORRECT
// File: AscendingOrderChecker.java
public class AscendingOrderChecker {
    public static boolean isSorted(int[] nums) {
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] < nums[i - 1]) {
                return false;
            }
        }

        return true;
    }
}

// File: AscendingOrderCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class AscendingOrderCheckerTest {
    @Test
    void testSorted() {
        assertTrue(
                AscendingOrderChecker.isSorted(
                        new int[]{1,2,3,4}
                )
        );
    }

    @Test
    void testNotSorted() {
        assertFalse(
                AscendingOrderChecker.isSorted(
                        new int[]{4,2,1}
                )
        );
    }

    @Test
    void testSingle() {
        assertTrue(
                AscendingOrderChecker.isSorted(
                        new int[]{7}
                )
        );
    }
}

### SAMPLE 1666
// Class: InvalidConstructorBuilder
// Status: COMPILATION_ERROR
// File: InvalidConstructorBuilder.java
public class InvalidConstructorBuilder {
    public WrongName() {

    }

    public static int cube(int n) {
        return n * n * n;
    }
}

// File: InvalidConstructorBuilderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidConstructorBuilderTest {
    @Test
    void testCube() {
        assertEquals(
                27,
                InvalidConstructorBuilder.cube(3)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                InvalidConstructorBuilder.cube(0)
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                -8,
                InvalidConstructorBuilder.cube(-2)
        );
    }
}
### SAMPLE 1667
// Class: BrokenMaximumFinder
// Status: BUGGY
// File: BrokenMaximumFinder.java
public class BrokenMaximumFinder {
    public static int max(int[] nums) {
        int max = Integer.MAX_VALUE;

        for (int n : nums) {
            if (n > max) {
                max = n;
            }
        }

        return max;
    }
}

// File: BrokenMaximumFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenMaximumFinderTest {
    @Test
    void testMax() {
        assertEquals(
                9,
                BrokenMaximumFinder.max(
                        new int[]{1,9,3}
                )
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                -1,
                BrokenMaximumFinder.max(
                        new int[]{-5,-1,-7}
                )
        );
    }

    @Test
    void testSingle() {
        assertEquals(
                4,
                BrokenMaximumFinder.max(
                        new int[]{4}
                )
        );
    }
}

### SAMPLE 1668
// Class: DivideByZeroSimulator
// Status: RUNTIME_ERROR
// File: DivideByZeroSimulator.java
public class DivideByZeroSimulator {
    public static int calculate(int n) {
        return n / 0;
    }
}

// File: DivideByZeroSimulatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DivideByZeroSimulatorTest {
    @Test
    void testValue() {
        assertEquals(
                5,
                DivideByZeroSimulator.calculate(5)
        );
    }

    @Test
    void testAnother() {
        assertEquals(
                8,
                DivideByZeroSimulator.calculate(8)
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                DivideByZeroSimulator.calculate(0)
        );
    }
}

### SAMPLE 1669
// Class: PrimeNumberUtility
// Status: CORRECT
// File: PrimeNumberUtility.java
public class PrimeNumberUtility {
    public static boolean isPrime(int n) {
        if (n <= 1) {
            return false;
        }

        for (int i = 2; i * i <= n; i++) {
            if (n % i == 0) {
                return false;
            }
        }

        return true;
    }
}

// File: PrimeNumberUtilityTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PrimeNumberUtilityTest {
    @Test
    void testPrime() {
        assertTrue(
                PrimeNumberUtility.isPrime(13)
        );
    }

    @Test
    void testNonPrime() {
        assertFalse(
                PrimeNumberUtility.isPrime(12)
        );
    }

    @Test
    void testOne() {
        assertFalse(
                PrimeNumberUtility.isPrime(1)
        );
    }
}

### SAMPLE 1670
// Class: InvalidVariableDeclaration
// Status: COMPILATION_ERROR
// File: InvalidVariableDeclaration.java
public class InvalidVariableDeclaration {
    public static int add() {
        int 1value = 5;
        return 1value;
    }
}

// File: InvalidVariableDeclarationTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidVariableDeclarationTest {
    @Test
    void testAdd() {
        assertEquals(
                5,
                InvalidVariableDeclaration.add()
        );
    }

    @Test
    void testAgain() {
        assertTrue(
                InvalidVariableDeclaration.add() > 0
        );
    }

    @Test
    void testNotZero() {
        assertNotEquals(
                0,
                InvalidVariableDeclaration.add()
        );
    }
}

### SAMPLE 1671
// Class: IncorrectPalindromeChecker
// Status: BUGGY
// File: IncorrectPalindromeChecker.java
public class IncorrectPalindromeChecker {
    public static boolean check(String s) {
        String reversed =
                new StringBuilder(s)
                        .reverse()
                        .toString();

        return s == reversed;
    }
}

// File: IncorrectPalindromeCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectPalindromeCheckerTest {
    @Test
    void testPalindrome() {
        assertTrue(
                IncorrectPalindromeChecker.check("madam")
        );
    }

    @Test
    void testNotPalindrome() {
        assertFalse(
                IncorrectPalindromeChecker.check("java")
        );
    }

    @Test
    void testSingle() {
        assertTrue(
                IncorrectPalindromeChecker.check("a")
        );
    }
}

### SAMPLE 1672
// Class: NegativeArrayCreator
// Status: RUNTIME_ERROR
// File: NegativeArrayCreator.java
public class NegativeArrayCreator {
    public static int[] create() {
        return new int[-5];
    }
}

// File: NegativeArrayCreatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NegativeArrayCreatorTest {
    @Test
    void testArray() {
        assertEquals(
                5,
                NegativeArrayCreator.create().length
        );
    }

    @Test
    void testNotNull() {
        assertNotNull(
                NegativeArrayCreator.create()
        );
    }

    @Test
    void testAgain() {
        assertTrue(
                NegativeArrayCreator.create().length > 0
        );
    }
}

### SAMPLE 1673
// Class: ArrayElementSummer
// Status: CORRECT
// File: ArrayElementSummer.java
public class ArrayElementSummer {
    public static int sum(int[] nums) {
        int total = 0;

        for (int n : nums) {
            total += n;
        }

        return total;
    }
}

// File: ArrayElementSummerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayElementSummerTest {
    @Test
    void testSum() {
        assertEquals(
                10,
                ArrayElementSummer.sum(
                        new int[]{1,2,3,4}
                )
        );
    }

    @Test
    void testZero() {
        assertEquals(
                0,
                ArrayElementSummer.sum(
                        new int[]{}
                )
        );
    }

    @Test
    void testNegative() {
        assertEquals(
                -6,
                ArrayElementSummer.sum(
                        new int[]{-1,-2,-3}
                )
        );
    }
}

### SAMPLE 1674
// Class: WrongEvenChecker
// Status: BUGGY
// File: WrongEvenChecker.java
public class WrongEvenChecker {
    public static boolean isEven(int n) {
        return n % 2 == 1;
    }
}

// File: WrongEvenCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class WrongEvenCheckerTest {
    @Test
    void testEven() {
        assertTrue(
                WrongEvenChecker.isEven(4)
        );
    }

    @Test
    void testOdd() {
        assertFalse(
                WrongEvenChecker.isEven(5)
        );
    }

    @Test
    void testZero() {
        assertTrue(
                WrongEvenChecker.isEven(0)
        );
    }
}
### SAMPLE 1693
// Class: InfiniteRecursionCounter
// Status: RUNTIME_ERROR
// File: InfiniteRecursionCounter.java
public class InfiniteRecursionCounter {
    public int countDown(int n) {
        return countDown(n - 1) + 1;
    }
}

// File: InfiniteRecursionCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InfiniteRecursionCounterTest {

    @Test
    void testCounter() {
        InfiniteRecursionCounter c =
                new InfiniteRecursionCounter();

        assertEquals(3, c.countDown(3));
    }

    @Test
    void testOne() {
        InfiniteRecursionCounter c =
                new InfiniteRecursionCounter();

        assertEquals(1, c.countDown(1));
    }
}

### SAMPLE 1694
// Class: ArrayPeakDetector
// Status: BUGGY
// File: ArrayPeakDetector.java
public class ArrayPeakDetector {

    public int findPeak(int[] nums) {

        for (int i = 1; i < nums.length - 1; i++) {

            if (nums[i] > nums[i - 1] &&
                    nums[i] > nums[i + 1]) {

                return nums[i];
            }
        }

        return -1;
    }
}

// File: ArrayPeakDetectorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayPeakDetectorTest {

    @Test
    void testPeak() {
        ArrayPeakDetector a =
                new ArrayPeakDetector();

        assertEquals(5,
                a.findPeak(new int[]{1,5,2}));
    }

    @Test
    void testEdgePeak() {
        ArrayPeakDetector a =
                new ArrayPeakDetector();

        assertEquals(10,
                a.findPeak(new int[]{10,2,1}));
    }
}

### SAMPLE 1695
// Class: QueueStackSimulator
// Status: CORRECT
// File: QueueStackSimulator.java
import java.util.LinkedList;
import java.util.Queue;

public class QueueStackSimulator {

    private Queue<Integer> queue =
            new LinkedList<>();

    public void push(int val) {

        queue.offer(val);

        for (int i = 0;
             i < queue.size() - 1;
             i++) {

            queue.offer(queue.poll());
        }
    }

    public int pop() {
        return queue.poll();
    }

    public int top() {
        return queue.peek();
    }
}

// File: QueueStackSimulatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class QueueStackSimulatorTest {

    @Test
    void testPushPop() {
        QueueStackSimulator q =
                new QueueStackSimulator();

        q.push(1);
        q.push(2);

        assertEquals(2, q.pop());
    }

    @Test
    void testTop() {
        QueueStackSimulator q =
                new QueueStackSimulator();

        q.push(7);

        assertEquals(7, q.top());
    }
}

### SAMPLE 1696
// Class: MissingReturnHandler
// Status: COMPILATION_ERROR
// File: MissingReturnHandler.java
public class MissingReturnHandler {

    public int add(int a, int b) {

        int sum = a + b;
    }
}

// File: MissingReturnHandlerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingReturnHandlerTest {

    @Test
    void testAdd() {
        MissingReturnHandler m =
                new MissingReturnHandler();

        assertEquals(5, m.add(2,3));
    }
}

### SAMPLE 1697
// Class: SlidingWindowMaximumFinder
// Status: BUGGY
// File: SlidingWindowMaximumFinder.java
public class SlidingWindowMaximumFinder {

    public int maxWindow(int[] nums,
                         int k) {

        int max = Integer.MIN_VALUE;

        for (int i = 0; i < k; i++) {
            max = Math.min(max, nums[i]);
        }

        return max;
    }
}

// File: SlidingWindowMaximumFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SlidingWindowMaximumFinderTest {

    @Test
    void testWindow() {
        SlidingWindowMaximumFinder s =
                new SlidingWindowMaximumFinder();

        assertEquals(5,
                s.maxWindow(
                        new int[]{1,5,2},
                        2
                ));
    }

    @Test
    void testAnother() {
        SlidingWindowMaximumFinder s =
                new SlidingWindowMaximumFinder();

        assertEquals(9,
                s.maxWindow(
                        new int[]{9,1},
                        1
                ));
    }
}

### SAMPLE 1698
// Class: LinkedListCycleInspector
// Status: CORRECT
// File: LinkedListCycleInspector.java
public class LinkedListCycleInspector {

    static class Node {
        int val;
        Node next;

        Node(int val) {
            this.val = val;
        }
    }

    public boolean hasCycle(Node head) {

        Node slow = head;
        Node fast = head;

        while (fast != null &&
                fast.next != null) {

            slow = slow.next;
            fast = fast.next.next;

            if (slow == fast) {
                return true;
            }
        }

        return false;
    }
}

// File: LinkedListCycleInspectorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class LinkedListCycleInspectorTest {

    @Test
    void testCycle() {
        LinkedListCycleInspector l =
                new LinkedListCycleInspector();

        LinkedListCycleInspector.Node a =
                new LinkedListCycleInspector.Node(1);

        LinkedListCycleInspector.Node b =
                new LinkedListCycleInspector.Node(2);

        a.next = b;
        b.next = a;

        assertTrue(l.hasCycle(a));
    }

    @Test
    void testNoCycle() {
        LinkedListCycleInspector l =
                new LinkedListCycleInspector();

        LinkedListCycleInspector.Node a =
                new LinkedListCycleInspector.Node(1);

        assertFalse(l.hasCycle(a));
    }
}

### SAMPLE 1699
// Class: NullStringLengthFinder
// Status: RUNTIME_ERROR
// File: NullStringLengthFinder.java
public class NullStringLengthFinder {

    public int length(String s) {
        return s.length();
    }
}

// File: NullStringLengthFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullStringLengthFinderTest {

    @Test
    void testLength() {
        NullStringLengthFinder n =
                new NullStringLengthFinder();

        assertEquals(4,
                n.length("java"));
    }

    @Test
    void testNull() {
        NullStringLengthFinder n =
                new NullStringLengthFinder();

        assertThrows(
                NullPointerException.class,
                () -> n.length(null)
        );
    }
}

### SAMPLE 1700
// Class: DuplicateCharacterRemover
// Status: BUGGY
// File: DuplicateCharacterRemover.java
public class DuplicateCharacterRemover {

    public String remove(String s) {

        String result = "";

        for (char c : s.toCharArray()) {

            if (result.indexOf(c) == -1) {
                result += c + c;
            }
        }

        return result;
    }
}

// File: DuplicateCharacterRemoverTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DuplicateCharacterRemoverTest {

    @Test
    void testRemove() {
        DuplicateCharacterRemover d =
                new DuplicateCharacterRemover();

        assertEquals("abc",
                d.remove("aabbcc"));
    }

    @Test
    void testSingle() {
        DuplicateCharacterRemover d =
                new DuplicateCharacterRemover();

        assertEquals("z",
                d.remove("z"));
    }
}

### SAMPLE 1701
// Class: MatrixDiagonalSummer
// Status: CORRECT
// File: MatrixDiagonalSummer.java
public class MatrixDiagonalSummer {

    public int diagonalSum(int[][] matrix) {

        int sum = 0;

        for (int i = 0;
             i < matrix.length;
             i++) {

            sum += matrix[i][i];
        }

        return sum;
    }
}

// File: MatrixDiagonalSummerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MatrixDiagonalSummerTest {

    @Test
    void testDiagonal() {
        MatrixDiagonalSummer m =
                new MatrixDiagonalSummer();

        assertEquals(
                15,
                m.diagonalSum(
                        new int[][]{
                                {1,2,3},
                                {4,5,6},
                                {7,8,9}
                        }
                )
        );
    }

    @Test
    void testSingle() {
        MatrixDiagonalSummer m =
                new MatrixDiagonalSummer();

        assertEquals(
                4,
                m.diagonalSum(
                        new int[][]{{4}}
                )
        );
    }
}

### SAMPLE 1702
// Class: InvalidArrayDeclaration
// Status: COMPILATION_ERROR
// File: InvalidArrayDeclaration.java
public class InvalidArrayDeclaration {

    public int total() {

        int[] arr =
                new int[5

        return arr.length;
    }
}

// File: InvalidArrayDeclarationTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidArrayDeclarationTest {

    @Test
    void testTotal() {
        InvalidArrayDeclaration i =
                new InvalidArrayDeclaration();

        assertEquals(5, i.total());
    }
}

### SAMPLE 1703
// Class: FibonacciSequenceBuilder
// Status: BUGGY
// File: FibonacciSequenceBuilder.java
public class FibonacciSequenceBuilder {

    public int fibonacci(int n) {

        if (n <= 1) {
            return 1;
        }

        return fibonacci(n - 1) +
                fibonacci(n - 2);
    }
}

// File: FibonacciSequenceBuilderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class FibonacciSequenceBuilderTest {

    @Test
    void testFib() {
        FibonacciSequenceBuilder f =
                new FibonacciSequenceBuilder();

        assertEquals(0,
                f.fibonacci(0));
    }

    @Test
    void testAnother() {
        FibonacciSequenceBuilder f =
                new FibonacciSequenceBuilder();

        assertEquals(5,
                f.fibonacci(5));
    }
}

### SAMPLE 1704
// Class: PalindromeSentenceValidator
// Status: CORRECT
// File: PalindromeSentenceValidator.java
public class PalindromeSentenceValidator {

    public boolean isPalindrome(String s) {

        String cleaned =
                s.replaceAll("[^a-zA-Z0-9]", "")
                        .toLowerCase();

        int left = 0;
        int right = cleaned.length() - 1;

        while (left < right) {

            if (cleaned.charAt(left) !=
                    cleaned.charAt(right)) {

                return false;
            }

            left++;
            right--;
        }

        return true;
    }
}

// File: PalindromeSentenceValidatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PalindromeSentenceValidatorTest {

    @Test
    void testPalindrome() {
        PalindromeSentenceValidator p =
                new PalindromeSentenceValidator();

        assertTrue(
                p.isPalindrome(
                        "A man, a plan, a canal: Panama"
                )
        );
    }

    @Test
    void testNotPalindrome() {
        PalindromeSentenceValidator p =
                new PalindromeSentenceValidator();

        assertFalse(
                p.isPalindrome("hello")
        );
    }
}

### SAMPLE 1705
// Class: DivisionWithoutCheck
// Status: RUNTIME_ERROR
// File: DivisionWithoutCheck.java
public class DivisionWithoutCheck {

    public int divide(int a, int b) {
        return a / b;
    }
}

// File: DivisionWithoutCheckTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DivisionWithoutCheckTest {

    @Test
    void testDivide() {
        DivisionWithoutCheck d =
                new DivisionWithoutCheck();

        assertEquals(2,
                d.divide(10,5));
    }

    @Test
    void testException() {
        DivisionWithoutCheck d =
                new DivisionWithoutCheck();

        assertThrows(
                ArithmeticException.class,
                () -> d.divide(5,0)
        );
    }
}
### SAMPLE 1671
// Class: ArrayMedianFinder
// Status: BUGGY
// File: ArrayMedianFinder.java
import java.util.Arrays;

public class ArrayMedianFinder {
    public double median(int[] nums) {
        Arrays.sort(nums);

        int n = nums.length;

        if (n % 2 == 0) {
            return nums[n / 2];
        }

        return (nums[n / 2] + nums[n / 2 - 1]) / 2.0;
    }
}

// File: ArrayMedianFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayMedianFinderTest {
    @Test
    void testOdd() {
        ArrayMedianFinder a =
                new ArrayMedianFinder();

        assertEquals(
                3.0,
                a.median(new int[]{1,3,5})
        );
    }

    @Test
    void testEven() {
        ArrayMedianFinder a =
                new ArrayMedianFinder();

        assertEquals(
                2.5,
                a.median(new int[]{1,2,3,4})
        );
    }

    @Test
    void testSingle() {
        ArrayMedianFinder a =
                new ArrayMedianFinder();

        assertEquals(
                7.0,
                a.median(new int[]{7})
        );
    }
}

### SAMPLE 1672
// Class: ReverseSentenceBuilder
// Status: CORRECT
// File: ReverseSentenceBuilder.java
public class ReverseSentenceBuilder {
    public String reverseWords(String s) {
        String[] arr = s.split(" ");

        StringBuilder sb =
                new StringBuilder();

        for (int i = arr.length - 1;
             i >= 0;
             i--) {

            sb.append(arr[i]);

            if (i != 0) {
                sb.append(" ");
            }
        }

        return sb.toString();
    }
}

// File: ReverseSentenceBuilderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ReverseSentenceBuilderTest {
    @Test
    void testReverse() {
        ReverseSentenceBuilder r =
                new ReverseSentenceBuilder();

        assertEquals(
                "world hello",
                r.reverseWords("hello world")
        );
    }

    @Test
    void testSingle() {
        ReverseSentenceBuilder r =
                new ReverseSentenceBuilder();

        assertEquals(
                "java",
                r.reverseWords("java")
        );
    }

    @Test
    void testThreeWords() {
        ReverseSentenceBuilder r =
                new ReverseSentenceBuilder();

        assertEquals(
                "c b a",
                r.reverseWords("a b c")
        );
    }
}

### SAMPLE 1673
// Class: BrokenLoopCounter
// Status: COMPILATION_ERROR
// File: BrokenLoopCounter.java
public class BrokenLoopCounter {
    public int count(int n) {
        int sum = 0

        for (int i = 1; i <= n; i++) {
            sum += i;
        }

        return sum;
    }
}

// File: BrokenLoopCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenLoopCounterTest {
    @Test
    void testCount() {
        BrokenLoopCounter b =
                new BrokenLoopCounter();

        assertEquals(6, b.count(3));
    }

    @Test
    void testZero() {
        BrokenLoopCounter b =
                new BrokenLoopCounter();

        assertEquals(0, b.count(0));
    }

    @Test
    void testFive() {
        BrokenLoopCounter b =
                new BrokenLoopCounter();

        assertEquals(15, b.count(5));
    }
}

### SAMPLE 1674
// Class: PrimeRangeCollector
// Status: BUGGY
// File: PrimeRangeCollector.java
import java.util.*;

public class PrimeRangeCollector {
    public List<Integer> collect(int n) {
        List<Integer> result =
                new ArrayList<>();

        for (int i = 2; i < n; i++) {
            boolean prime = true;

            for (int j = 2;
                 j < Math.sqrt(i);
                 j++) {

                if (i % j == 0) {
                    prime = false;
                    break;
                }
            }

            if (prime) {
                result.add(i);
            }
        }

        return result;
    }
}

// File: PrimeRangeCollectorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.*;

class PrimeRangeCollectorTest {
    @Test
    void testPrimes() {
        PrimeRangeCollector p =
                new PrimeRangeCollector();

        List<Integer> result =
                p.collect(10);

        assertTrue(result.contains(7));
    }

    @Test
    void testComposite() {
        PrimeRangeCollector p =
                new PrimeRangeCollector();

        List<Integer> result =
                p.collect(30);

        assertFalse(result.contains(25));
    }

    @Test
    void testSmall() {
        PrimeRangeCollector p =
                new PrimeRangeCollector();

        assertEquals(
                0,
                p.collect(2).size()
        );
    }
}

### SAMPLE 1675
// Class: InfiniteRecursionCrash
// Status: RUNTIME_ERROR
// File: InfiniteRecursionCrash.java
public class InfiniteRecursionCrash {
    public int factorial(int n) {
        return n * factorial(n - 1);
    }
}

// File: InfiniteRecursionCrashTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InfiniteRecursionCrashTest {
    @Test
    void testFactorial() {
        InfiniteRecursionCrash i =
                new InfiniteRecursionCrash();

        assertEquals(
                120,
                i.factorial(5)
        );
    }

    @Test
    void testOne() {
        InfiniteRecursionCrash i =
                new InfiniteRecursionCrash();

        assertEquals(
                1,
                i.factorial(1)
        );
    }

    @Test
    void testZero() {
        InfiniteRecursionCrash i =
                new InfiniteRecursionCrash();

        assertEquals(
                1,
                i.factorial(0)
        );
    }
}

### SAMPLE 1676
// Class: MatrixDiagonalSummer
// Status: CORRECT
// File: MatrixDiagonalSummer.java
public class MatrixDiagonalSummer {
    public int sum(int[][] matrix) {
        int total = 0;

        for (int i = 0;
             i < matrix.length;
             i++) {

            total += matrix[i][i];
        }

        return total;
    }
}

// File: MatrixDiagonalSummerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MatrixDiagonalSummerTest {
    @Test
    void testMatrix() {
        MatrixDiagonalSummer m =
                new MatrixDiagonalSummer();

        assertEquals(
                15,
                m.sum(
                        new int[][]{
                                {1,2,3},
                                {4,5,6},
                                {7,8,9}
                        }
                )
        );
    }

    @Test
    void testSingle() {
        MatrixDiagonalSummer m =
                new MatrixDiagonalSummer();

        assertEquals(
                4,
                m.sum(new int[][]{{4}})
        );
    }

    @Test
    void testZero() {
        MatrixDiagonalSummer m =
                new MatrixDiagonalSummer();

        assertEquals(
                0,
                m.sum(
                        new int[][]{
                                {0,0},
                                {0,0}
                        }
                )
        );
    }
}

### SAMPLE 1677
// Class: NullStringLengthFinder
// Status: RUNTIME_ERROR
// File: NullStringLengthFinder.java
public class NullStringLengthFinder {
    public int length(String s) {
        return s.length();
    }
}

// File: NullStringLengthFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullStringLengthFinderTest {
    @Test
    void testWord() {
        NullStringLengthFinder n =
                new NullStringLengthFinder();

        assertEquals(
                5,
                n.length("hello")
        );
    }

    @Test
    void testEmpty() {
        NullStringLengthFinder n =
                new NullStringLengthFinder();

        assertEquals(
                0,
                n.length("")
        );
    }

    @Test
    void testNull() {
        NullStringLengthFinder n =
                new NullStringLengthFinder();

        assertThrows(
                NullPointerException.class,
                () -> n.length(null)
        );
    }
}

### SAMPLE 1678
// Class: CharacterFrequencyMap
// Status: CORRECT
// File: CharacterFrequencyMap.java
import java.util.*;

public class CharacterFrequencyMap {
    public Map<Character,Integer> build(
            String s) {

        Map<Character,Integer> map =
                new HashMap<>();

        for (char c : s.toCharArray()) {
            map.put(
                    c,
                    map.getOrDefault(c,0) + 1
            );
        }

        return map;
    }
}

// File: CharacterFrequencyMapTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.*;

class CharacterFrequencyMapTest {
    @Test
    void testFrequency() {
        CharacterFrequencyMap c =
                new CharacterFrequencyMap();

        Map<Character,Integer> map =
                c.build("apple");

        assertEquals(2, map.get('p'));
    }

    @Test
    void testSingle() {
        CharacterFrequencyMap c =
                new CharacterFrequencyMap();

        Map<Character,Integer> map =
                c.build("a");

        assertEquals(1, map.get('a'));
    }

    @Test
    void testEmpty() {
        CharacterFrequencyMap c =
                new CharacterFrequencyMap();

        Map<Character,Integer> map =
                c.build("");

        assertEquals(0, map.size());
    }
}

### SAMPLE 1679
// Class: IncorrectArrayIndexer
// Status: BUGGY
// File: IncorrectArrayIndexer.java
public class IncorrectArrayIndexer {
    public int last(int[] nums) {
        return nums[nums.length];
    }
}

// File: IncorrectArrayIndexerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectArrayIndexerTest {
    @Test
    void testLast() {
        IncorrectArrayIndexer i =
                new IncorrectArrayIndexer();

        assertEquals(
                5,
                i.last(new int[]{1,2,5})
        );
    }

    @Test
    void testSingle() {
        IncorrectArrayIndexer i =
                new IncorrectArrayIndexer();

        assertEquals(
                9,
                i.last(new int[]{9})
        );
    }

    @Test
    void testTwo() {
        IncorrectArrayIndexer i =
                new IncorrectArrayIndexer();

        assertEquals(
                3,
                i.last(new int[]{2,3})
        );
    }
}

### SAMPLE 1680
// Class: BalancedParenthesisChecker
// Status: CORRECT
// File: BalancedParenthesisChecker.java
import java.util.Stack;

public class BalancedParenthesisChecker {
    public boolean valid(String s) {
        Stack<Character> stack =
                new Stack<>();

        for (char c : s.toCharArray()) {
            if (c == '(') {
                stack.push(c);
            } else if (c == ')') {
                if (stack.isEmpty()) {
                    return false;
                }

                stack.pop();
            }
        }

        return stack.isEmpty();
    }
}

// File: BalancedParenthesisCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BalancedParenthesisCheckerTest {
    @Test
    void testValid() {
        BalancedParenthesisChecker b =
                new BalancedParenthesisChecker();

        assertTrue(b.valid("(())"));
    }

    @Test
    void testInvalid() {
        BalancedParenthesisChecker b =
                new BalancedParenthesisChecker();

        assertFalse(b.valid("(()"));
    }

    @Test
    void testEmpty() {
        BalancedParenthesisChecker b =
                new BalancedParenthesisChecker();

        assertTrue(b.valid(""));
    }
}
### SAMPLE 1681
// Class: DuplicateValueLocator
// Status: BUGGY
// File: DuplicateValueLocator.java
import java.util.HashSet;
import java.util.Set;

public class DuplicateValueLocator {
    public boolean containsDuplicate(int[] nums) {
        Set<Integer> set =
                new HashSet<>();

        for (int n : nums) {
            if (set.contains(n + 1)) {
                return true;
            }

            set.add(n);
        }

        return false;
    }
}

// File: DuplicateValueLocatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DuplicateValueLocatorTest {
    @Test
    void testDuplicate() {
        DuplicateValueLocator d =
                new DuplicateValueLocator();

        assertTrue(
                d.containsDuplicate(
                        new int[]{1,2,3,1}
                )
        );
    }

    @Test
    void testNoDuplicate() {
        DuplicateValueLocator d =
                new DuplicateValueLocator();

        assertFalse(
                d.containsDuplicate(
                        new int[]{1,2,3,4}
                )
        );
    }

    @Test
    void testSingle() {
        DuplicateValueLocator d =
                new DuplicateValueLocator();

        assertFalse(
                d.containsDuplicate(
                        new int[]{7}
                )
        );
    }
}

### SAMPLE 1682
// Class: LinkedNodeReverser
// Status: CORRECT
// File: LinkedNodeReverser.java
public class LinkedNodeReverser {
    static class Node {
        int val;
        Node next;

        Node(int val) {
            this.val = val;
        }
    }

    public Node reverse(Node head) {
        Node prev = null;

        while (head != null) {
            Node next = head.next;
            head.next = prev;
            prev = head;
            head = next;
        }

        return prev;
    }
}

// File: LinkedNodeReverserTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class LinkedNodeReverserTest {
    @Test
    void testReverse() {
        LinkedNodeReverser l =
                new LinkedNodeReverser();

        LinkedNodeReverser.Node a =
                new LinkedNodeReverser.Node(1);

        LinkedNodeReverser.Node b =
                new LinkedNodeReverser.Node(2);

        a.next = b;

        LinkedNodeReverser.Node result =
                l.reverse(a);

        assertEquals(2, result.val);
    }

    @Test
    void testSingle() {
        LinkedNodeReverser l =
                new LinkedNodeReverser();

        LinkedNodeReverser.Node a =
                new LinkedNodeReverser.Node(5);

        LinkedNodeReverser.Node result =
                l.reverse(a);

        assertEquals(5, result.val);
    }

    @Test
    void testNull() {
        LinkedNodeReverser l =
                new LinkedNodeReverser();

        assertNull(l.reverse(null));
    }
}

### SAMPLE 1683
// Class: InvalidReturnBuilder
// Status: COMPILATION_ERROR
// File: InvalidReturnBuilder.java
public class InvalidReturnBuilder {
    public int build() {
        return "hello";
    }
}

// File: InvalidReturnBuilderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidReturnBuilderTest {
    @Test
    void testBuild() {
        InvalidReturnBuilder i =
                new InvalidReturnBuilder();

        assertEquals(1, i.build());
    }

    @Test
    void testAgain() {
        InvalidReturnBuilder i =
                new InvalidReturnBuilder();

        assertEquals(1, i.build());
    }

    @Test
    void testAnother() {
        InvalidReturnBuilder i =
                new InvalidReturnBuilder();

        assertEquals(1, i.build());
    }
}

### SAMPLE 1684
// Class: FibonacciSequenceMaker
// Status: BUGGY
// File: FibonacciSequenceMaker.java
public class FibonacciSequenceMaker {
    public int fibonacci(int n) {
        if (n <= 1) {
            return 1;
        }

        return fibonacci(n - 1)
                + fibonacci(n - 2);
    }
}

// File: FibonacciSequenceMakerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class FibonacciSequenceMakerTest {
    @Test
    void testZero() {
        FibonacciSequenceMaker f =
                new FibonacciSequenceMaker();

        assertEquals(0, f.fibonacci(0));
    }

    @Test
    void testOne() {
        FibonacciSequenceMaker f =
                new FibonacciSequenceMaker();

        assertEquals(1, f.fibonacci(1));
    }

    @Test
    void testFive() {
        FibonacciSequenceMaker f =
                new FibonacciSequenceMaker();

        assertEquals(5, f.fibonacci(5));
    }
}

### SAMPLE 1685
// Class: DivideByZeroGenerator
// Status: RUNTIME_ERROR
// File: DivideByZeroGenerator.java
public class DivideByZeroGenerator {
    public int divide(int a) {
        return a / 0;
    }
}

// File: DivideByZeroGeneratorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DivideByZeroGeneratorTest {
    @Test
    void testDivide() {
        DivideByZeroGenerator d =
                new DivideByZeroGenerator();

        assertThrows(
                ArithmeticException.class,
                () -> d.divide(5)
        );
    }

    @Test
    void testAnother() {
        DivideByZeroGenerator d =
                new DivideByZeroGenerator();

        assertThrows(
                ArithmeticException.class,
                () -> d.divide(10)
        );
    }

    @Test
    void testNegative() {
        DivideByZeroGenerator d =
                new DivideByZeroGenerator();

        assertThrows(
                ArithmeticException.class,
                () -> d.divide(-1)
        );
    }
}

### SAMPLE 1686
// Class: IntervalOverlapChecker
// Status: CORRECT
// File: IntervalOverlapChecker.java
public class IntervalOverlapChecker {
    public boolean overlap(
            int s1,
            int e1,
            int s2,
            int e2) {

        return Math.max(s1, s2)
                <= Math.min(e1, e2);
    }
}

// File: IntervalOverlapCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IntervalOverlapCheckerTest {
    @Test
    void testOverlap() {
        IntervalOverlapChecker i =
                new IntervalOverlapChecker();

        assertTrue(
                i.overlap(1,5,4,8)
        );
    }

    @Test
    void testNoOverlap() {
        IntervalOverlapChecker i =
                new IntervalOverlapChecker();

        assertFalse(
                i.overlap(1,2,3,4)
        );
    }

    @Test
    void testEdgeTouch() {
        IntervalOverlapChecker i =
                new IntervalOverlapChecker();

        assertTrue(
                i.overlap(1,3,3,6)
        );
    }
}

### SAMPLE 1687
// Class: NegativeIndexReader
// Status: RUNTIME_ERROR
// File: NegativeIndexReader.java
public class NegativeIndexReader {
    public int read(int[] nums) {
        return nums[-1];
    }
}

// File: NegativeIndexReaderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NegativeIndexReaderTest {
    @Test
    void testRead() {
        NegativeIndexReader n =
                new NegativeIndexReader();

        assertThrows(
                ArrayIndexOutOfBoundsException.class,
                () -> n.read(new int[]{1,2,3})
        );
    }

    @Test
    void testSingle() {
        NegativeIndexReader n =
                new NegativeIndexReader();

        assertThrows(
                ArrayIndexOutOfBoundsException.class,
                () -> n.read(new int[]{5})
        );
    }

    @Test
    void testAnother() {
        NegativeIndexReader n =
                new NegativeIndexReader();

        assertThrows(
                ArrayIndexOutOfBoundsException.class,
                () -> n.read(new int[]{7,8})
        );
    }
}

### SAMPLE 1688
// Class: StringPalindromeInspector
// Status: CORRECT
// File: StringPalindromeInspector.java
public class StringPalindromeInspector {
    public boolean check(String s) {
        int left = 0;
        int right = s.length() - 1;

        while (left < right) {
            if (s.charAt(left)
                    != s.charAt(right)) {

                return false;
            }

            left++;
            right--;
        }

        return true;
    }
}

// File: StringPalindromeInspectorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class StringPalindromeInspectorTest {
    @Test
    void testPalindrome() {
        StringPalindromeInspector s =
                new StringPalindromeInspector();

        assertTrue(s.check("madam"));
    }

    @Test
    void testNotPalindrome() {
        StringPalindromeInspector s =
                new StringPalindromeInspector();

        assertFalse(s.check("java"));
    }

    @Test
    void testSingle() {
        StringPalindromeInspector s =
                new StringPalindromeInspector();

        assertTrue(s.check("a"));
    }
}

### SAMPLE 1689
// Class: MissingTypeSemicolon
// Status: COMPILATION_ERROR
// File: MissingTypeSemicolon.java
public class MissingTypeSemicolon {
    public int square(int x) {
        int value = x * x
        return value;
    }
}

// File: MissingTypeSemicolonTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingTypeSemicolonTest {
    @Test
    void testSquare() {
        MissingTypeSemicolon m =
                new MissingTypeSemicolon();

        assertEquals(16, m.square(4));
    }

    @Test
    void testZero() {
        MissingTypeSemicolon m =
                new MissingTypeSemicolon();

        assertEquals(0, m.square(0));
    }

    @Test
    void testNegative() {
        MissingTypeSemicolon m =
                new MissingTypeSemicolon();

        assertEquals(9, m.square(-3));
    }
}

### SAMPLE 1690
// Class: SortedMergeProcessor
// Status: BUGGY
// File: SortedMergeProcessor.java
public class SortedMergeProcessor {
    public int[] merge(
            int[] a,
            int[] b) {

        int[] result =
                new int[a.length + b.length];

        int i = 0;
        int j = 0;
        int k = 0;

        while (i < a.length &&
                j < b.length) {

            if (a[i] < b[j]) {
                result[k++] = a[i++];
            } else {
                result[k++] = a[i++];
            }
        }

        return result;
    }
}

// File: SortedMergeProcessorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SortedMergeProcessorTest {
    @Test
    void testMerge() {
        SortedMergeProcessor s =
                new SortedMergeProcessor();

        int[] result =
                s.merge(
                        new int[]{1,3},
                        new int[]{2,4}
                );

        assertEquals(1, result[0]);
    }

    @Test
    void testLength() {
        SortedMergeProcessor s =
                new SortedMergeProcessor();

        int[] result =
                s.merge(
                        new int[]{1},
                        new int[]{2}
                );

        assertEquals(2, result.length);
    }

    @Test
    void testAnother() {
        SortedMergeProcessor s =
                new SortedMergeProcessor();

        int[] result =
                s.merge(
                        new int[]{5,6},
                        new int[]{1,2}
                );

        assertEquals(1, result[0]);
    }
}
### SAMPLE 1691
// Class: QueueUsingStacks
// Status: CORRECT
// File: QueueUsingStacks.java
import java.util.Stack;

public class QueueUsingStacks {
    private Stack<Integer> input =
            new Stack<>();

    private Stack<Integer> output =
            new Stack<>();

    public void push(int x) {
        input.push(x);
    }

    public int pop() {
        shift();

        return output.pop();
    }

    public int peek() {
        shift();

        return output.peek();
    }

    private void shift() {
        if (output.isEmpty()) {
            while (!input.isEmpty()) {
                output.push(input.pop());
            }
        }
    }
}

// File: QueueUsingStacksTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class QueueUsingStacksTest {
    @Test
    void testPushPop() {
        QueueUsingStacks q =
                new QueueUsingStacks();

        q.push(10);
        q.push(20);

        assertEquals(10, q.pop());
    }

    @Test
    void testPeek() {
        QueueUsingStacks q =
                new QueueUsingStacks();

        q.push(5);

        assertEquals(5, q.peek());
    }

    @Test
    void testOrder() {
        QueueUsingStacks q =
                new QueueUsingStacks();

        q.push(1);
        q.push(2);
        q.push(3);

        q.pop();

        assertEquals(2, q.peek());
    }
}

### SAMPLE 1692
// Class: IncorrectAverageCalculator
// Status: BUGGY
// File: IncorrectAverageCalculator.java
public class IncorrectAverageCalculator {
    public double average(int[] nums) {
        int sum = 0;

        for (int n : nums) {
            sum += n;
        }

        return sum / (nums.length + 1);
    }
}

// File: IncorrectAverageCalculatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectAverageCalculatorTest {
    @Test
    void testAverage() {
        IncorrectAverageCalculator i =
                new IncorrectAverageCalculator();

        assertEquals(
                2.0,
                i.average(new int[]{1,2,3}),
                0.001
        );
    }

    @Test
    void testSingle() {
        IncorrectAverageCalculator i =
                new IncorrectAverageCalculator();

        assertEquals(
                5.0,
                i.average(new int[]{5}),
                0.001
        );
    }

    @Test
    void testLarge() {
        IncorrectAverageCalculator i =
                new IncorrectAverageCalculator();

        assertEquals(
                4.0,
                i.average(new int[]{2,4,6}),
                0.001
        );
    }
}

### SAMPLE 1693
// Class: InvalidBooleanReturn
// Status: COMPILATION_ERROR
// File: InvalidBooleanReturn.java
public class InvalidBooleanReturn {
    public boolean valid() {
        return 1;
    }
}

// File: InvalidBooleanReturnTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidBooleanReturnTest {
    @Test
    void testValid() {
        InvalidBooleanReturn i =
                new InvalidBooleanReturn();

        assertTrue(i.valid());
    }

    @Test
    void testAgain() {
        InvalidBooleanReturn i =
                new InvalidBooleanReturn();

        assertTrue(i.valid());
    }

    @Test
    void testThird() {
        InvalidBooleanReturn i =
                new InvalidBooleanReturn();

        assertTrue(i.valid());
    }
}

### SAMPLE 1694
// Class: BinaryConverterTool
// Status: BUGGY
// File: BinaryConverterTool.java
public class BinaryConverterTool {
    public String convert(int n) {
        StringBuilder sb =
                new StringBuilder();

        while (n > 0) {
            sb.append(n % 2);
            n /= 2;
        }

        return sb.toString();
    }
}

// File: BinaryConverterToolTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BinaryConverterToolTest {
    @Test
    void testBinary() {
        BinaryConverterTool b =
                new BinaryConverterTool();

        assertEquals(
                "101",
                b.convert(5)
        );
    }

    @Test
    void testTwo() {
        BinaryConverterTool b =
                new BinaryConverterTool();

        assertEquals(
                "10",
                b.convert(2)
        );
    }

    @Test
    void testOne() {
        BinaryConverterTool b =
                new BinaryConverterTool();

        assertEquals(
                "1",
                b.convert(1)
        );
    }
}

### SAMPLE 1695
// Class: NullArrayCrash
// Status: RUNTIME_ERROR
// File: NullArrayCrash.java
public class NullArrayCrash {
    public int size(int[] nums) {
        return nums.length;
    }
}

// File: NullArrayCrashTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullArrayCrashTest {
    @Test
    void testArray() {
        NullArrayCrash n =
                new NullArrayCrash();

        assertEquals(
                3,
                n.size(new int[]{1,2,3})
        );
    }

    @Test
    void testEmpty() {
        NullArrayCrash n =
                new NullArrayCrash();

        assertEquals(
                0,
                n.size(new int[]{})
        );
    }

    @Test
    void testNull() {
        NullArrayCrash n =
                new NullArrayCrash();

        assertThrows(
                NullPointerException.class,
                () -> n.size(null)
        );
    }
}

### SAMPLE 1696
// Class: UniqueCharacterChecker
// Status: CORRECT
// File: UniqueCharacterChecker.java
import java.util.HashSet;
import java.util.Set;

public class UniqueCharacterChecker {
    public boolean unique(String s) {
        Set<Character> set =
                new HashSet<>();

        for (char c : s.toCharArray()) {
            if (set.contains(c)) {
                return false;
            }

            set.add(c);
        }

        return true;
    }
}

// File: UniqueCharacterCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class UniqueCharacterCheckerTest {
    @Test
    void testUnique() {
        UniqueCharacterChecker u =
                new UniqueCharacterChecker();

        assertTrue(u.unique("abc"));
    }

    @Test
    void testDuplicate() {
        UniqueCharacterChecker u =
                new UniqueCharacterChecker();

        assertFalse(u.unique("hello"));
    }

    @Test
    void testEmpty() {
        UniqueCharacterChecker u =
                new UniqueCharacterChecker();

        assertTrue(u.unique(""));
    }
}

### SAMPLE 1697
// Class: BrokenConstructorExample
// Status: COMPILATION_ERROR
// File: BrokenConstructorExample.java
public class BrokenConstructorExample {
    public BrokenConstructorExample {
    }

    public int value() {
        return 10;
    }
}

// File: BrokenConstructorExampleTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenConstructorExampleTest {
    @Test
    void testValue() {
        BrokenConstructorExample b =
                new BrokenConstructorExample();

        assertEquals(10, b.value());
    }

    @Test
    void testAgain() {
        BrokenConstructorExample b =
                new BrokenConstructorExample();

        assertEquals(10, b.value());
    }

    @Test
    void testThird() {
        BrokenConstructorExample b =
                new BrokenConstructorExample();

        assertEquals(10, b.value());
    }
}

### SAMPLE 1698
// Class: MaxElementScanner
// Status: BUGGY
// File: MaxElementScanner.java
public class MaxElementScanner {
    public int max(int[] nums) {
        int max = 0;

        for (int n : nums) {
            if (n > max) {
                max = n;
            }
        }

        return max;
    }
}

// File: MaxElementScannerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MaxElementScannerTest {
    @Test
    void testPositive() {
        MaxElementScanner m =
                new MaxElementScanner();

        assertEquals(
                9,
                m.max(new int[]{1,9,2})
        );
    }

    @Test
    void testNegative() {
        MaxElementScanner m =
                new MaxElementScanner();

        assertEquals(
                -1,
                m.max(new int[]{-5,-1,-9})
        );
    }

    @Test
    void testSingle() {
        MaxElementScanner m =
                new MaxElementScanner();

        assertEquals(
                7,
                m.max(new int[]{7})
        );
    }
}

### SAMPLE 1699
// Class: StackOverflowLooper
// Status: RUNTIME_ERROR
// File: StackOverflowLooper.java
public class StackOverflowLooper {
    public void loop() {
        loop();
    }
}

// File: StackOverflowLooperTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class StackOverflowLooperTest {
    @Test
    void testLoop() {
        StackOverflowLooper s =
                new StackOverflowLooper();

        assertThrows(
                StackOverflowError.class,
                () -> s.loop()
        );
    }

    @Test
    void testAgain() {
        StackOverflowLooper s =
                new StackOverflowLooper();

        assertThrows(
                StackOverflowError.class,
                () -> s.loop()
        );
    }

    @Test
    void testThird() {
        StackOverflowLooper s =
                new StackOverflowLooper();

        assertThrows(
                StackOverflowError.class,
                () -> s.loop()
        );
    }
}

### SAMPLE 1700
// Class: VowelCounterUtility
// Status: CORRECT
// File: VowelCounterUtility.java
public class VowelCounterUtility {
    public int count(String s) {
        int total = 0;

        for (char c : s.toLowerCase().toCharArray()) {
            if ("aeiou".indexOf(c) >= 0) {
                total++;
            }
        }

        return total;
    }
}

// File: VowelCounterUtilityTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class VowelCounterUtilityTest {
    @Test
    void testCount() {
        VowelCounterUtility v =
                new VowelCounterUtility();

        assertEquals(
                2,
                v.count("hello")
        );
    }

    @Test
    void testUpperCase() {
        VowelCounterUtility v =
                new VowelCounterUtility();

        assertEquals(
                5,
                v.count("AEIOU")
        );
    }

    @Test
    void testNoVowels() {
        VowelCounterUtility v =
                new VowelCounterUtility();

        assertEquals(
                0,
                v.count("rhythm")
        );
    }
}
### SAMPLE 1711
// Class: RecursiveListSummer
// Status: RUNTIME_ERROR
// File: RecursiveListSummer.java
public class RecursiveListSummer {
    public int sum(int[] arr, int index) {
        return arr[index] + sum(arr, index + 1);
    }
}

// File: RecursiveListSummerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class RecursiveListSummerTest {
    @Test
    void testSum() {
        RecursiveListSummer r =
                new RecursiveListSummer();

        assertEquals(
                10,
                r.sum(new int[]{1,2,3,4},0)
        );
    }

    @Test
    void testSingle() {
        RecursiveListSummer r =
                new RecursiveListSummer();

        assertEquals(
                5,
                r.sum(new int[]{5},0)
        );
    }
}

### SAMPLE 1712
// Class: AlternateCharacterMerger
// Status: CORRECT
// File: AlternateCharacterMerger.java
public class AlternateCharacterMerger {
    public String merge(String a, String b) {
        StringBuilder sb =
                new StringBuilder();

        int i = 0;

        while (i < a.length() ||
                i < b.length()) {

            if (i < a.length()) {
                sb.append(a.charAt(i));
            }

            if (i < b.length()) {
                sb.append(b.charAt(i));
            }

            i++;
        }

        return sb.toString();
    }
}

// File: AlternateCharacterMergerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class AlternateCharacterMergerTest {
    @Test
    void testMerge() {
        AlternateCharacterMerger a =
                new AlternateCharacterMerger();

        assertEquals(
                "a1b2c3",
                a.merge("abc","123")
        );
    }

    @Test
    void testDifferentLengths() {
        AlternateCharacterMerger a =
                new AlternateCharacterMerger();

        assertEquals(
                "a1b23",
                a.merge("ab","123")
        );
    }
}

### SAMPLE 1713
// Class: BrokenLoopPrinter
// Status: COMPILATION_ERROR
// File: BrokenLoopPrinter.java
public class BrokenLoopPrinter {
    public void printNumbers() {
        for (int i = 0; i < 5; i++
            System.out.println(i);
        }
    }
}

// File: BrokenLoopPrinterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenLoopPrinterTest {
    @Test
    void testPrint() {
        BrokenLoopPrinter b =
                new BrokenLoopPrinter();

        b.printNumbers();
    }
}

### SAMPLE 1714
// Class: PairDifferenceChecker
// Status: BUGGY
// File: PairDifferenceChecker.java
public class PairDifferenceChecker {
    public boolean hasDifference(
            int[] arr,
            int target
    ) {
        for (int i = 0; i < arr.length; i++) {
            for (int j = i + 1;
                 j < arr.length;
                 j++) {

                if (arr[i] - arr[j] == target) {
                    return true;
                }
            }
        }

        return false;
    }
}

// File: PairDifferenceCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PairDifferenceCheckerTest {
    @Test
    void testDifference() {
        PairDifferenceChecker p =
                new PairDifferenceChecker();

        assertTrue(
                p.hasDifference(
                        new int[]{1,5,9},
                        4
                )
        );
    }

    @Test
    void testNoDifference() {
        PairDifferenceChecker p =
                new PairDifferenceChecker();

        assertFalse(
                p.hasDifference(
                        new int[]{2,4,6},
                        5
                )
        );
    }
}

### SAMPLE 1715
// Class: StringChunkReverser
// Status: CORRECT
// File: StringChunkReverser.java
public class StringChunkReverser {
    public String reverseChunks(
            String s,
            int k
    ) {
        StringBuilder result =
                new StringBuilder();

        for (int i = 0;
             i < s.length();
             i += k) {

            int end =
                    Math.min(i + k, s.length());

            String chunk =
                    s.substring(i, end);

            result.append(
                    new StringBuilder(chunk)
                            .reverse()
            );
        }

        return result.toString();
    }
}

// File: StringChunkReverserTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class StringChunkReverserTest {
    @Test
    void testReverse() {
        StringChunkReverser s =
                new StringChunkReverser();

        assertEquals(
                "cbafed",
                s.reverseChunks("abcdef",3)
        );
    }

    @Test
    void testUneven() {
        StringChunkReverser s =
                new StringChunkReverser();

        assertEquals(
                "bacd",
                s.reverseChunks("abcd",2)
        );
    }
}

### SAMPLE 1716
// Class: NullArrayTraverser
// Status: RUNTIME_ERROR
// File: NullArrayTraverser.java
public class NullArrayTraverser {
    public int length(int[] arr) {
        return arr.length;
    }
}

// File: NullArrayTraverserTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullArrayTraverserTest {
    @Test
    void testLength() {
        NullArrayTraverser n =
                new NullArrayTraverser();

        assertEquals(
                3,
                n.length(new int[]{1,2,3})
        );
    }

    @Test
    void testNull() {
        NullArrayTraverser n =
                new NullArrayTraverser();

        assertThrows(
                NullPointerException.class,
                () -> n.length(null)
        );
    }
}

### SAMPLE 1717
// Class: ConsecutiveNumberCounter
// Status: BUGGY
// File: ConsecutiveNumberCounter.java
public class ConsecutiveNumberCounter {
    public int countConsecutive(int[] arr) {
        int count = 0;

        for (int i = 0;
             i < arr.length - 1;
             i++) {

            if (arr[i] + 1 == arr[i + 1]) {
                count++;
            }
        }

        return count + 1;
    }
}

// File: ConsecutiveNumberCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ConsecutiveNumberCounterTest {
    @Test
    void testCount() {
        ConsecutiveNumberCounter c =
                new ConsecutiveNumberCounter();

        assertEquals(
                2,
                c.countConsecutive(
                        new int[]{1,2,4,5}
                )
        );
    }

    @Test
    void testEmpty() {
        ConsecutiveNumberCounter c =
                new ConsecutiveNumberCounter();

        assertEquals(
                0,
                c.countConsecutive(new int[]{})
        );
    }
}

### SAMPLE 1718
// Class: CircularStringShift
// Status: CORRECT
// File: CircularStringShift.java
public class CircularStringShift {
    public String shift(
            String s,
            int k
    ) {
        if (s.isEmpty()) {
            return s;
        }

        k = k % s.length();

        return s.substring(
                s.length() - k
        ) + s.substring(
                0,
                s.length() - k
        );
    }
}

// File: CircularStringShiftTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CircularStringShiftTest {
    @Test
    void testShift() {
        CircularStringShift c =
                new CircularStringShift();

        assertEquals(
                "deabc",
                c.shift("abcde",2)
        );
    }

    @Test
    void testZero() {
        CircularStringShift c =
                new CircularStringShift();

        assertEquals(
                "abc",
                c.shift("abc",0)
        );
    }
}

### SAMPLE 1719
// Class: ArrayBoundaryCrash
// Status: RUNTIME_ERROR
// File: ArrayBoundaryCrash.java
public class ArrayBoundaryCrash {
    public int access(int[] arr) {
        return arr[arr.length];
    }
}

// File: ArrayBoundaryCrashTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayBoundaryCrashTest {
    @Test
    void testCrash() {
        ArrayBoundaryCrash a =
                new ArrayBoundaryCrash();

        assertThrows(
                ArrayIndexOutOfBoundsException.class,
                () -> a.access(new int[]{1,2,3})
        );
    }
}

### SAMPLE 1720
// Class: InvalidBooleanMethod
// Status: COMPILATION_ERROR
// File: InvalidBooleanMethod.java
public class InvalidBooleanMethod {
    public boolean check(int x) {
        if (x > 0) {
            return true;
        }
    }
}

// File: InvalidBooleanMethodTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidBooleanMethodTest {
    @Test
    void testCheck() {
        InvalidBooleanMethod i =
                new InvalidBooleanMethod();

        assertTrue(i.check(5));
    }
}

### SAMPLE 1721
// Class: ReverseDigitBuilder
// Status: BUGGY
// File: ReverseDigitBuilder.java
public class ReverseDigitBuilder {
    public int reverse(int n) {
        int rev = 0;

        while (n > 0) {
            rev += n % 10;
            n /= 10;
        }

        return rev;
    }
}

// File: ReverseDigitBuilderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ReverseDigitBuilderTest {
    @Test
    void testReverse() {
        ReverseDigitBuilder r =
                new ReverseDigitBuilder();

        assertEquals(
                321,
                r.reverse(123)
        );
    }

    @Test
    void testSingle() {
        ReverseDigitBuilder r =
                new ReverseDigitBuilder();

        assertEquals(
                7,
                r.reverse(7)
        );
    }
}

### SAMPLE 1722
// Class: OddIndexSummer
// Status: CORRECT
// File: OddIndexSummer.java
public class OddIndexSummer {
    public int sum(int[] arr) {
        int total = 0;

        for (int i = 1;
             i < arr.length;
             i += 2) {

            total += arr[i];
        }

        return total;
    }
}

// File: OddIndexSummerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class OddIndexSummerTest {
    @Test
    void testOddSum() {
        OddIndexSummer o =
                new OddIndexSummer();

        assertEquals(
                6,
                o.sum(new int[]{1,2,3,4})
        );
    }

    @Test
    void testEmpty() {
        OddIndexSummer o =
                new OddIndexSummer();

        assertEquals(
                0,
                o.sum(new int[]{})
        );
    }
}

### SAMPLE 1723
// Class: InvalidArrayDeclaration
// Status: COMPILATION_ERROR
// File: InvalidArrayDeclaration.java
public class InvalidArrayDeclaration {
    public int[] create() {
        int arr = new int[5];
        return arr;
    }
}

// File: InvalidArrayDeclarationTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidArrayDeclarationTest {
    @Test
    void testArray() {
        InvalidArrayDeclaration i =
                new InvalidArrayDeclaration();

        assertEquals(
                5,
                i.create().length
        );
    }
}

### SAMPLE 1724
// Class: DuplicateCharacterFinder
// Status: BUGGY
// File: DuplicateCharacterFinder.java
import java.util.HashSet;

public class DuplicateCharacterFinder {
    public boolean hasDuplicate(String s) {
        HashSet<Character> set =
                new HashSet<>();

        for (char c : s.toCharArray()) {
            if (set.contains(c)) {
                return false;
            }

            set.add(c);
        }

        return true;
    }
}

// File: DuplicateCharacterFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DuplicateCharacterFinderTest {
    @Test
    void testDuplicate() {
        DuplicateCharacterFinder d =
                new DuplicateCharacterFinder();

        assertTrue(
                d.hasDuplicate("hello")
        );
    }

    @Test
    void testNoDuplicate() {
        DuplicateCharacterFinder d =
                new DuplicateCharacterFinder();

        assertFalse(
                d.hasDuplicate("abc")
        );
    }
}

### SAMPLE 1725
// Class: VowelSequenceCounter
// Status: CORRECT
// File: VowelSequenceCounter.java
public class VowelSequenceCounter {
    public int count(String s) {
        int total = 0;

        for (char c : s.toLowerCase().toCharArray()) {
            if ("aeiou".indexOf(c) >= 0) {
                total++;
            }
        }

        return total;
    }
}

// File: VowelSequenceCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class VowelSequenceCounterTest {
    @Test
    void testCount() {
        VowelSequenceCounter v =
                new VowelSequenceCounter();

        assertEquals(
                2,
                v.count("hello")
        );
    }

    @Test
    void testUpperCase() {
        VowelSequenceCounter v =
                new VowelSequenceCounter();

        assertEquals(
                5,
                v.count("AEIOU")
        );
    }
}
### SAMPLE 1726
// Class: InfiniteCounterLoop
// Status: RUNTIME_ERROR
// File: InfiniteCounterLoop.java
public class InfiniteCounterLoop {
    public int count() {
        int i = 0;

        while (true) {
            i++;
        }
    }
}

// File: InfiniteCounterLoopTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InfiniteCounterLoopTest {
    @Test
    void testCount() {
        InfiniteCounterLoop i =
                new InfiniteCounterLoop();

        assertTimeoutPreemptively(
                java.time.Duration.ofMillis(100),
                () -> i.count()
        );
    }
}

### SAMPLE 1727
// Class: SortedPairFinder
// Status: CORRECT
// File: SortedPairFinder.java
public class SortedPairFinder {
    public boolean hasPair(
            int[] arr,
            int target
    ) {
        int left = 0;
        int right = arr.length - 1;

        while (left < right) {
            int sum =
                    arr[left] + arr[right];

            if (sum == target) {
                return true;
            }

            if (sum < target) {
                left++;
            } else {
                right--;
            }
        }

        return false;
    }
}

// File: SortedPairFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SortedPairFinderTest {
    @Test
    void testPairExists() {
        SortedPairFinder s =
                new SortedPairFinder();

        assertTrue(
                s.hasPair(
                        new int[]{1,2,3,4,5},
                        9
                )
        );
    }

    @Test
    void testNoPair() {
        SortedPairFinder s =
                new SortedPairFinder();

        assertFalse(
                s.hasPair(
                        new int[]{1,2,3},
                        10
                )
        );
    }
}

### SAMPLE 1728
// Class: WrongReturnTypeMethod
// Status: COMPILATION_ERROR
// File: WrongReturnTypeMethod.java
public class WrongReturnTypeMethod {
    public int getName() {
        return "John";
    }
}

// File: WrongReturnTypeMethodTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class WrongReturnTypeMethodTest {
    @Test
    void testName() {
        WrongReturnTypeMethod w =
                new WrongReturnTypeMethod();

        assertEquals(
                "John",
                w.getName()
        );
    }
}

### SAMPLE 1729
// Class: BrokenPalindromeChecker
// Status: BUGGY
// File: BrokenPalindromeChecker.java
public class BrokenPalindromeChecker {
    public boolean isPalindrome(String s) {
        int left = 0;
        int right = s.length() - 1;

        while (left < right) {
            if (s.charAt(left) !=
                    s.charAt(right)) {
                return true;
            }

            left++;
            right--;
        }

        return false;
    }
}

// File: BrokenPalindromeCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenPalindromeCheckerTest {
    @Test
    void testPalindrome() {
        BrokenPalindromeChecker b =
                new BrokenPalindromeChecker();

        assertTrue(
                b.isPalindrome("level")
        );
    }

    @Test
    void testNotPalindrome() {
        BrokenPalindromeChecker b =
                new BrokenPalindromeChecker();

        assertFalse(
                b.isPalindrome("java")
        );
    }
}

### SAMPLE 1730
// Class: PositiveNumberCounter
// Status: CORRECT
// File: PositiveNumberCounter.java
public class PositiveNumberCounter {
    public int count(int[] arr) {
        int total = 0;

        for (int n : arr) {
            if (n > 0) {
                total++;
            }
        }

        return total;
    }
}

// File: PositiveNumberCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PositiveNumberCounterTest {
    @Test
    void testPositiveCount() {
        PositiveNumberCounter p =
                new PositiveNumberCounter();

        assertEquals(
                3,
                p.count(
                        new int[]{1,-1,5,0,8}
                )
        );
    }

    @Test
    void testNone() {
        PositiveNumberCounter p =
                new PositiveNumberCounter();

        assertEquals(
                0,
                p.count(
                        new int[]{-1,-2,0}
                )
        );
    }
}

### SAMPLE 1731
// Class: EmptyStringCrash
// Status: RUNTIME_ERROR
// File: EmptyStringCrash.java
public class EmptyStringCrash {
    public char first(String s) {
        return s.charAt(0);
    }
}

// File: EmptyStringCrashTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class EmptyStringCrashTest {
    @Test
    void testFirstCharacter() {
        EmptyStringCrash e =
                new EmptyStringCrash();

        assertEquals(
                'a',
                e.first("abc")
        );
    }

    @Test
    void testEmpty() {
        EmptyStringCrash e =
                new EmptyStringCrash();

        assertThrows(
                StringIndexOutOfBoundsException.class,
                () -> e.first("")
        );
    }
}

### SAMPLE 1732
// Class: DescendingOrderSorter
// Status: BUGGY
// File: DescendingOrderSorter.java
import java.util.Arrays;

public class DescendingOrderSorter {
    public int[] sort(int[] arr) {
        Arrays.sort(arr);
        return arr;
    }
}

// File: DescendingOrderSorterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DescendingOrderSorterTest {
    @Test
    void testSort() {
        DescendingOrderSorter d =
                new DescendingOrderSorter();

        assertArrayEquals(
                new int[]{5,4,3,1},
                d.sort(new int[]{3,5,1,4})
        );
    }

    @Test
    void testSingle() {
        DescendingOrderSorter d =
                new DescendingOrderSorter();

        assertArrayEquals(
                new int[]{7},
                d.sort(new int[]{7})
        );
    }
}

### SAMPLE 1733
// Class: CharacterFrequencyCounter
// Status: CORRECT
// File: CharacterFrequencyCounter.java
import java.util.HashMap;
import java.util.Map;

public class CharacterFrequencyCounter {
    public Map<Character,Integer> count(
            String s
    ) {
        Map<Character,Integer> map =
                new HashMap<>();

        for (char c : s.toCharArray()) {
            map.put(
                    c,
                    map.getOrDefault(c,0) + 1
            );
        }

        return map;
    }
}

// File: CharacterFrequencyCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CharacterFrequencyCounterTest {
    @Test
    void testFrequency() {
        CharacterFrequencyCounter c =
                new CharacterFrequencyCounter();

        assertEquals(
                2,
                c.count("hello").get('l')
        );
    }

    @Test
    void testSingle() {
        CharacterFrequencyCounter c =
                new CharacterFrequencyCounter();

        assertEquals(
                1,
                c.count("a").get('a')
        );
    }
}

### SAMPLE 1734
// Class: InvalidForLoopClass
// Status: COMPILATION_ERROR
// File: InvalidForLoopClass.java
public class InvalidForLoopClass {
    public void loop() {
        for int i = 0; i < 5; i++) {
            System.out.println(i);
        }
    }
}

// File: InvalidForLoopClassTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidForLoopClassTest {
    @Test
    void testLoop() {
        InvalidForLoopClass i =
                new InvalidForLoopClass();

        i.loop();
    }
}

### SAMPLE 1735
// Class: DuplicateValueRemover
// Status: BUGGY
// File: DuplicateValueRemover.java
import java.util.ArrayList;
import java.util.List;

public class DuplicateValueRemover {
    public List<Integer> remove(
            int[] arr
    ) {
        List<Integer> list =
                new ArrayList<>();

        for (int n : arr) {
            list.add(n);
        }

        return list;
    }
}

// File: DuplicateValueRemoverTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.*;

class DuplicateValueRemoverTest {
    @Test
    void testRemoveDuplicates() {
        DuplicateValueRemover d =
                new DuplicateValueRemover();

        assertEquals(
                Arrays.asList(1,2,3),
                d.remove(new int[]{1,2,2,3})
        );
    }

    @Test
    void testUnique() {
        DuplicateValueRemover d =
                new DuplicateValueRemover();

        assertEquals(
                Arrays.asList(4,5),
                d.remove(new int[]{4,5})
        );
    }
}

### SAMPLE 1736
// Class: MatrixDiagonalAdder
// Status: CORRECT
// File: MatrixDiagonalAdder.java
public class MatrixDiagonalAdder {
    public int sum(int[][] matrix) {
        int total = 0;

        for (int i = 0;
             i < matrix.length;
             i++) {

            total += matrix[i][i];
        }

        return total;
    }
}

// File: MatrixDiagonalAdderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MatrixDiagonalAdderTest {
    @Test
    void testDiagonalSum() {
        MatrixDiagonalAdder m =
                new MatrixDiagonalAdder();

        assertEquals(
                15,
                m.sum(
                        new int[][]{
                                {1,2,3},
                                {4,5,6},
                                {7,8,9}
                        }
                )
        );
    }

    @Test
    void testSingle() {
        MatrixDiagonalAdder m =
                new MatrixDiagonalAdder();

        assertEquals(
                5,
                m.sum(new int[][]{{5}})
        );
    }
}

### SAMPLE 1737
// Class: NegativeArrayAllocator
// Status: RUNTIME_ERROR
// File: NegativeArrayAllocator.java
public class NegativeArrayAllocator {
    public int[] create(int size) {
        return new int[size];
    }
}

// File: NegativeArrayAllocatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NegativeArrayAllocatorTest {
    @Test
    void testValidSize() {
        NegativeArrayAllocator n =
                new NegativeArrayAllocator();

        assertEquals(
                5,
                n.create(5).length
        );
    }

    @Test
    void testNegativeSize() {
        NegativeArrayAllocator n =
                new NegativeArrayAllocator();

        assertThrows(
                NegativeArraySizeException.class,
                () -> n.create(-2)
        );
    }
}

### SAMPLE 1738
// Class: EvenDigitCounter
// Status: BUGGY
// File: EvenDigitCounter.java
public class EvenDigitCounter {
    public int countEvenDigits(int n) {
        int count = 0;

        while (n > 0) {
            int digit = n % 10;

            if (digit % 2 == 1) {
                count++;
            }

            n /= 10;
        }

        return count;
    }
}

// File: EvenDigitCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class EvenDigitCounterTest {
    @Test
    void testEvenDigits() {
        EvenDigitCounter e =
                new EvenDigitCounter();

        assertEquals(
                2,
                e.countEvenDigits(248)
        );
    }

    @Test
    void testNoEvenDigits() {
        EvenDigitCounter e =
                new EvenDigitCounter();

        assertEquals(
                0,
                e.countEvenDigits(135)
        );
    }
}

### SAMPLE 1739
// Class: SpaceRemover
// Status: CORRECT
// File: SpaceRemover.java
public class SpaceRemover {
    public String remove(String s) {
        return s.replace(" ","");
    }
}

// File: SpaceRemoverTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SpaceRemoverTest {
    @Test
    void testRemoveSpaces() {
        SpaceRemover s =
                new SpaceRemover();

        assertEquals(
                "helloworld",
                s.remove("hello world")
        );
    }

    @Test
    void testNoSpaces() {
        SpaceRemover s =
                new SpaceRemover();

        assertEquals(
                "java",
                s.remove("java")
        );
    }
}

### SAMPLE 1740
// Class: MissingSemicolonClass
// Status: COMPILATION_ERROR
// File: MissingSemicolonClass.java
public class MissingSemicolonClass {
    public int add(int a, int b) {
        int sum = a + b
        return sum;
    }
}

// File: MissingSemicolonClassTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingSemicolonClassTest {
    @Test
    void testAdd() {
        MissingSemicolonClass m =
                new MissingSemicolonClass();

        assertEquals(
                5,
                m.add(2,3)
        );
    }
}
### SAMPLE 1741
// Class: ReverseWordSequence
// Status: BUGGY
// File: ReverseWordSequence.java
public class ReverseWordSequence {
    public String reverseWords(String s) {
        String[] parts = s.split(" ");
        StringBuilder result =
                new StringBuilder();

        for (String word : parts) {
            result.append(word)
                    .append(" ");
        }

        return result.toString().trim();
    }
}

// File: ReverseWordSequenceTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ReverseWordSequenceTest {
    @Test
    void testReverseWords() {
        ReverseWordSequence r =
                new ReverseWordSequence();

        assertEquals(
                "world hello",
                r.reverseWords("hello world")
        );
    }

    @Test
    void testSingleWord() {
        ReverseWordSequence r =
                new ReverseWordSequence();

        assertEquals(
                "java",
                r.reverseWords("java")
        );
    }
}

### SAMPLE 1742
// Class: RecursiveFactorCounter
// Status: CORRECT
// File: RecursiveFactorCounter.java
public class RecursiveFactorCounter {
    public int countFactors(int n) {
        return helper(n,1);
    }

    private int helper(int n, int i) {
        if (i > n) {
            return 0;
        }

        if (n % i == 0) {
            return 1 + helper(n, i + 1);
        }

        return helper(n, i + 1);
    }
}

// File: RecursiveFactorCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class RecursiveFactorCounterTest {
    @Test
    void testFactors() {
        RecursiveFactorCounter r =
                new RecursiveFactorCounter();

        assertEquals(
                6,
                r.countFactors(12)
        );
    }

    @Test
    void testPrime() {
        RecursiveFactorCounter r =
                new RecursiveFactorCounter();

        assertEquals(
                2,
                r.countFactors(7)
        );
    }
}

### SAMPLE 1743
// Class: DivisionWithoutCheck
// Status: RUNTIME_ERROR
// File: DivisionWithoutCheck.java
public class DivisionWithoutCheck {
    public int divide(int a, int b) {
        return a / b;
    }
}

// File: DivisionWithoutCheckTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DivisionWithoutCheckTest {
    @Test
    void testDivide() {
        DivisionWithoutCheck d =
                new DivisionWithoutCheck();

        assertEquals(
                2,
                d.divide(10,5)
        );
    }

    @Test
    void testZeroDivision() {
        DivisionWithoutCheck d =
                new DivisionWithoutCheck();

        assertThrows(
                ArithmeticException.class,
                () -> d.divide(5,0)
        );
    }
}

### SAMPLE 1744
// Class: BrokenSwitchCase
// Status: COMPILATION_ERROR
// File: BrokenSwitchCase.java
public class BrokenSwitchCase {
    public String getDay(int n) {
        switch (n) {
            case 1
                return "Monday";
            default:
                return "Invalid";
        }
    }
}

// File: BrokenSwitchCaseTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenSwitchCaseTest {
    @Test
    void testDay() {
        BrokenSwitchCase b =
                new BrokenSwitchCase();

        assertEquals(
                "Monday",
                b.getDay(1)
        );
    }
}

### SAMPLE 1745
// Class: DuplicateAdjacentChecker
// Status: BUGGY
// File: DuplicateAdjacentChecker.java
public class DuplicateAdjacentChecker {
    public boolean hasAdjacentDuplicates(
            int[] arr
    ) {
        for (int i = 0;
             i < arr.length - 1;
             i++) {

            if (arr[i] == arr[i + 1]) {
                return false;
            }
        }

        return true;
    }
}

// File: DuplicateAdjacentCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DuplicateAdjacentCheckerTest {
    @Test
    void testHasDuplicates() {
        DuplicateAdjacentChecker d =
                new DuplicateAdjacentChecker();

        assertTrue(
                d.hasAdjacentDuplicates(
                        new int[]{1,2,2,3}
                )
        );
    }

    @Test
    void testNoDuplicates() {
        DuplicateAdjacentChecker d =
                new DuplicateAdjacentChecker();

        assertFalse(
                d.hasAdjacentDuplicates(
                        new int[]{1,2,3}
                )
        );
    }
}

### SAMPLE 1746
// Class: MaxElementLocator
// Status: CORRECT
// File: MaxElementLocator.java
public class MaxElementLocator {
    public int max(int[] arr) {
        int max = arr[0];

        for (int n : arr) {
            if (n > max) {
                max = n;
            }
        }

        return max;
    }
}

// File: MaxElementLocatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MaxElementLocatorTest {
    @Test
    void testMax() {
        MaxElementLocator m =
                new MaxElementLocator();

        assertEquals(
                9,
                m.max(new int[]{1,9,3})
        );
    }

    @Test
    void testNegative() {
        MaxElementLocator m =
                new MaxElementLocator();

        assertEquals(
                -1,
                m.max(new int[]{-5,-1,-7})
        );
    }
}

### SAMPLE 1747
// Class: NullStringSplitter
// Status: RUNTIME_ERROR
// File: NullStringSplitter.java
public class NullStringSplitter {
    public String[] split(String s) {
        return s.split(",");
    }
}

// File: NullStringSplitterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullStringSplitterTest {
    @Test
    void testSplit() {
        NullStringSplitter n =
                new NullStringSplitter();

        assertEquals(
                3,
                n.split("a,b,c").length
        );
    }

    @Test
    void testNull() {
        NullStringSplitter n =
                new NullStringSplitter();

        assertThrows(
                NullPointerException.class,
                () -> n.split(null)
        );
    }
}

### SAMPLE 1748
// Class: AscendingOrderValidator
// Status: BUGGY
// File: AscendingOrderValidator.java
public class AscendingOrderValidator {
    public boolean isAscending(int[] arr) {
        for (int i = 0;
             i < arr.length - 1;
             i++) {

            if (arr[i] < arr[i + 1]) {
                return false;
            }
        }

        return true;
    }
}

// File: AscendingOrderValidatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class AscendingOrderValidatorTest {
    @Test
    void testAscending() {
        AscendingOrderValidator a =
                new AscendingOrderValidator();

        assertTrue(
                a.isAscending(
                        new int[]{1,2,3,4}
                )
        );
    }

    @Test
    void testDescending() {
        AscendingOrderValidator a =
                new AscendingOrderValidator();

        assertFalse(
                a.isAscending(
                        new int[]{4,3,2}
                )
        );
    }
}

### SAMPLE 1749
// Class: FibonacciSequenceBuilder
// Status: CORRECT
// File: FibonacciSequenceBuilder.java
public class FibonacciSequenceBuilder {
    public int fib(int n) {
        if (n <= 1) {
            return n;
        }

        int a = 0;
        int b = 1;

        for (int i = 2; i <= n; i++) {
            int temp = a + b;
            a = b;
            b = temp;
        }

        return b;
    }
}

// File: FibonacciSequenceBuilderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class FibonacciSequenceBuilderTest {
    @Test
    void testFib() {
        FibonacciSequenceBuilder f =
                new FibonacciSequenceBuilder();

        assertEquals(
                13,
                f.fib(7)
        );
    }

    @Test
    void testBaseCases() {
        FibonacciSequenceBuilder f =
                new FibonacciSequenceBuilder();

        assertEquals(0, f.fib(0));
        assertEquals(1, f.fib(1));
    }
}

### SAMPLE 1750
// Class: InvalidIfCondition
// Status: COMPILATION_ERROR
// File: InvalidIfCondition.java
public class InvalidIfCondition {
    public boolean check(int x) {
        if x > 0 {
            return true;
        }

        return false;
    }
}

// File: InvalidIfConditionTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InvalidIfConditionTest {
    @Test
    void testCheck() {
        InvalidIfCondition i =
                new InvalidIfCondition();

        assertTrue(i.check(5));
    }
}

### SAMPLE 1751
// Class: LargestDigitFinder
// Status: BUGGY
// File: LargestDigitFinder.java
public class LargestDigitFinder {
    public int largest(int n) {
        int max = 9;

        while (n > 0) {
            int digit = n % 10;

            if (digit > max) {
                max = digit;
            }

            n /= 10;
        }

        return max;
    }
}

// File: LargestDigitFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class LargestDigitFinderTest {
    @Test
    void testLargestDigit() {
        LargestDigitFinder l =
                new LargestDigitFinder();

        assertEquals(
                8,
                l.largest(5821)
        );
    }

    @Test
    void testSingleDigit() {
        LargestDigitFinder l =
                new LargestDigitFinder();

        assertEquals(
                7,
                l.largest(7)
        );
    }
}

### SAMPLE 1752
// Class: MatrixRowSummer
// Status: CORRECT
// File: MatrixRowSummer.java
public class MatrixRowSummer {
    public int[] rowSums(int[][] matrix) {
        int[] result =
                new int[matrix.length];

        for (int i = 0;
             i < matrix.length;
             i++) {

            int sum = 0;

            for (int j = 0;
                 j < matrix[i].length;
                 j++) {

                sum += matrix[i][j];
            }

            result[i] = sum;
        }

        return result;
    }
}

// File: MatrixRowSummerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MatrixRowSummerTest {
    @Test
    void testRowSums() {
        MatrixRowSummer m =
                new MatrixRowSummer();

        assertArrayEquals(
                new int[]{6,15},
                m.rowSums(
                        new int[][]{
                                {1,2,3},
                                {4,5,6}
                        }
                )
        );
    }

    @Test
    void testSingleRow() {
        MatrixRowSummer m =
                new MatrixRowSummer();

        assertArrayEquals(
                new int[]{3},
                m.rowSums(
                        new int[][]{{1,2}}
                )
        );
    }
}

### SAMPLE 1753
// Class: StackOverflowRecursion
// Status: RUNTIME_ERROR
// File: StackOverflowRecursion.java
public class StackOverflowRecursion {
    public int recurse(int n) {
        return recurse(n + 1);
    }
}

// File: StackOverflowRecursionTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class StackOverflowRecursionTest {
    @Test
    void testRecursion() {
        StackOverflowRecursion s =
                new StackOverflowRecursion();

        assertThrows(
                StackOverflowError.class,
                () -> s.recurse(1)
        );
    }
}

### SAMPLE 1754
// Class: WrongArrayIndexFinder
// Status: BUGGY
// File: WrongArrayIndexFinder.java
public class WrongArrayIndexFinder {
    public int firstEven(int[] arr) {
        for (int i = 0;
             i < arr.length;
             i++) {

            if (arr[i] % 2 == 0) {
                return i + 1;
            }
        }

        return -1;
    }
}

// File: WrongArrayIndexFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class WrongArrayIndexFinderTest {
    @Test
    void testFirstEven() {
        WrongArrayIndexFinder w =
                new WrongArrayIndexFinder();

        assertEquals(
                1,
                w.firstEven(
                        new int[]{1,4,6}
                )
        );
    }

    @Test
    void testNoEven() {
        WrongArrayIndexFinder w =
                new WrongArrayIndexFinder();

        assertEquals(
                -1,
                w.firstEven(
                        new int[]{1,3,5}
                )
        );
    }
}

### SAMPLE 1755
// Class: CharacterCaseSwapper
// Status: CORRECT
// File: CharacterCaseSwapper.java
public class CharacterCaseSwapper {
    public String swap(String s) {
        StringBuilder result =
                new StringBuilder();

        for (char c : s.toCharArray()) {
            if (Character.isUpperCase(c)) {
                result.append(
                        Character.toLowerCase(c)
                );
            } else {
                result.append(
                        Character.toUpperCase(c)
                );
            }
        }

        return result.toString();
    }
}

// File: CharacterCaseSwapperTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CharacterCaseSwapperTest {
    @Test
    void testSwap() {
        CharacterCaseSwapper c =
                new CharacterCaseSwapper();

        assertEquals(
                "hELLO",
                c.swap("Hello")
        );
    }

    @Test
    void testUppercase() {
        CharacterCaseSwapper c =
                new CharacterCaseSwapper();

        assertEquals(
                "java",
                c.swap("JAVA")
        );
    }
}
### SAMPLE 1711
// Class: MatrixSpiralCollector
// Status: BUGGY
// File: MatrixSpiralCollector.java
import java.util.*;

public class MatrixSpiralCollector {
    public List<Integer> spiral(int[][] matrix) {
        List<Integer> result = new ArrayList<>();

        int top = 0;
        int bottom = matrix.length - 1;
        int left = 0;
        int right = matrix[0].length - 1;

        while (top < bottom && left < right) { // BUG

            for (int i = left; i <= right; i++) {
                result.add(matrix[top][i]);
            }

            top++;

            for (int i = top; i <= bottom; i++) {
                result.add(matrix[i][right]);
            }

            right--;

            for (int i = right; i >= left; i--) {
                result.add(matrix[bottom][i]);
            }

            bottom--;

            for (int i = bottom; i >= top; i--) {
                result.add(matrix[i][left]);
            }

            left++;
        }

        return result;
    }
}

// File: MatrixSpiralCollectorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.*;

class MatrixSpiralCollectorTest {

    @Test
    void testSpiral() {
        MatrixSpiralCollector m =
                new MatrixSpiralCollector();

        List<Integer> result =
                m.spiral(
                        new int[][]{
                                {1,2,3},
                                {4,5,6},
                                {7,8,9}
                        }
                );

        assertEquals(9, result.size());
    }

    @Test
    void testSingle() {
        MatrixSpiralCollector m =
                new MatrixSpiralCollector();

        List<Integer> result =
                m.spiral(new int[][]{{5}});

        assertEquals(1, result.size());
    }
}

### SAMPLE 1712
// Class: BrokenArrayRotator
// Status: COMPILATION_ERROR
// File: BrokenArrayRotator.java
public class BrokenArrayRotator {

    public void rotate(int[] nums, int k) {

        for (int i = 0; i < nums.length; i++) {
            nums[(i + k) % nums.length] = nums[i];
        }

// File: BrokenArrayRotatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenArrayRotatorTest {

    @Test
    void testRotate() {
        BrokenArrayRotator b =
                new BrokenArrayRotator();

        int[] arr = {1,2,3};

        b.rotate(arr,1);

        assertEquals(3, arr[0]);
    }
}

### SAMPLE 1713
// Class: QueueUsingStacks
// Status: CORRECT
// File: QueueUsingStacks.java
import java.util.*;

public class QueueUsingStacks {

    private Stack<Integer> s1 =
            new Stack<>();

    private Stack<Integer> s2 =
            new Stack<>();

    public void push(int x) {
        s1.push(x);
    }

    public int pop() {

        if (s2.isEmpty()) {

            while (!s1.isEmpty()) {
                s2.push(s1.pop());
            }
        }

        return s2.pop();
    }

    public int peek() {

        if (s2.isEmpty()) {

            while (!s1.isEmpty()) {
                s2.push(s1.pop());
            }
        }

        return s2.peek();
    }
}

// File: QueueUsingStacksTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class QueueUsingStacksTest {

    @Test
    void testQueue() {
        QueueUsingStacks q =
                new QueueUsingStacks();

        q.push(10);
        q.push(20);

        assertEquals(10, q.pop());
    }

    @Test
    void testPeek() {
        QueueUsingStacks q =
                new QueueUsingStacks();

        q.push(5);

        assertEquals(5, q.peek());
    }
}

### SAMPLE 1714
// Class: InfiniteRecursionAdder
// Status: RUNTIME_ERROR
// File: InfiniteRecursionAdder.java
public class InfiniteRecursionAdder {

    public int add(int a, int b) {
        return add(a,b);
    }
}

// File: InfiniteRecursionAdderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class InfiniteRecursionAdderTest {

    @Test
    void testAdd() {
        InfiniteRecursionAdder i =
                new InfiniteRecursionAdder();

        assertEquals(7, i.add(3,4));
    }
}

### SAMPLE 1715
// Class: DuplicateWordCounter
// Status: BUGGY
// File: DuplicateWordCounter.java
import java.util.*;

public class DuplicateWordCounter {

    public int countDuplicates(String[] words) {

        Set<String> set =
                new HashSet<>();

        int duplicates = 0;

        for (String w : words) {

            if (set.contains(w)) {
                duplicates++;
            }

            set.add(w);
        }

        return duplicates + 1; // BUG
    }
}

// File: DuplicateWordCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DuplicateWordCounterTest {

    @Test
    void testDuplicates() {
        DuplicateWordCounter d =
                new DuplicateWordCounter();

        assertEquals(
                2,
                d.countDuplicates(
                        new String[]{
                                "a","b","a","c","b"
                        }
                )
        );
    }

    @Test
    void testUnique() {
        DuplicateWordCounter d =
                new DuplicateWordCounter();

        assertEquals(
                0,
                d.countDuplicates(
                        new String[]{
                                "x","y"
                        }
                )
        );
    }
}

### SAMPLE 1716
// Class: SortedArrayMerger
// Status: CORRECT
// File: SortedArrayMerger.java
public class SortedArrayMerger {

    public int[] merge(int[] a, int[] b) {

        int[] result =
                new int[a.length + b.length];

        int i = 0;
        int j = 0;
        int k = 0;

        while (i < a.length &&
                j < b.length) {

            if (a[i] < b[j]) {
                result[k++] = a[i++];
            } else {
                result[k++] = b[j++];
            }
        }

        while (i < a.length) {
            result[k++] = a[i++];
        }

        while (j < b.length) {
            result[k++] = b[j++];
        }

        return result;
    }
}

// File: SortedArrayMergerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SortedArrayMergerTest {

    @Test
    void testMerge() {
        SortedArrayMerger s =
                new SortedArrayMerger();

        int[] result =
                s.merge(
                        new int[]{1,3,5},
                        new int[]{2,4,6}
                );

        assertEquals(6, result.length);
        assertEquals(1, result[0]);
        assertEquals(6, result[5]);
    }
}

### SAMPLE 1717
// Class: NegativeIndexFetcher
// Status: RUNTIME_ERROR
// File: NegativeIndexFetcher.java
public class NegativeIndexFetcher {

    public int fetch(int[] arr) {
        return arr[-1];
    }
}

// File: NegativeIndexFetcherTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NegativeIndexFetcherTest {

    @Test
    void testFetch() {
        NegativeIndexFetcher n =
                new NegativeIndexFetcher();

        assertEquals(
                3,
                n.fetch(new int[]{1,2,3})
        );
    }
}

### SAMPLE 1718
// Class: PalindromeSentenceChecker
// Status: CORRECT
// File: PalindromeSentenceChecker.java
public class PalindromeSentenceChecker {

    public boolean check(String text) {

        text =
                text.toLowerCase()
                        .replaceAll("[^a-z0-9]","");

        int left = 0;
        int right = text.length() - 1;

        while (left < right) {

            if (text.charAt(left) !=
                    text.charAt(right)) {
                return false;
            }

            left++;
            right--;
        }

        return true;
    }
}

// File: PalindromeSentenceCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class PalindromeSentenceCheckerTest {

    @Test
    void testPalindrome() {
        PalindromeSentenceChecker p =
                new PalindromeSentenceChecker();

        assertTrue(
                p.check("A man a plan a canal Panama")
        );
    }

    @Test
    void testNotPalindrome() {
        PalindromeSentenceChecker p =
                new PalindromeSentenceChecker();

        assertFalse(
                p.check("hello")
        );
    }
}

### SAMPLE 1719
// Class: IncorrectLoopSum
// Status: BUGGY
// File: IncorrectLoopSum.java
public class IncorrectLoopSum {

    public int sum(int n) {

        int total = 0;

        for (int i = 1; i < n; i++) { // BUG
            total += i;
        }

        return total;
    }
}

// File: IncorrectLoopSumTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectLoopSumTest {

    @Test
    void testSum() {
        IncorrectLoopSum i =
                new IncorrectLoopSum();

        assertEquals(15, i.sum(5));
    }

    @Test
    void testOne() {
        IncorrectLoopSum i =
                new IncorrectLoopSum();

        assertEquals(1, i.sum(1));
    }
}

### SAMPLE 1720
// Class: NullLengthCalculator
// Status: RUNTIME_ERROR
// File: NullLengthCalculator.java
public class NullLengthCalculator {

    public int length(String s) {
        return s.length();
    }
}

// File: NullLengthCalculatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NullLengthCalculatorTest {

    @Test
    void testLength() {
        NullLengthCalculator n =
                new NullLengthCalculator();

        assertEquals(5, n.length("hello"));
    }

    @Test
    void testNull() {
        NullLengthCalculator n =
                new NullLengthCalculator();

        assertThrows(
                NullPointerException.class,
                () -> n.length(null)
        );
    }
}
### SAMPLE 1721
// Class: EvenNumberFilter
// Status: BUGGY
// File: EvenNumberFilter.java
import java.util.*;

public class EvenNumberFilter {

    public List<Integer> filter(int[] nums) {

        List<Integer> result =
                new ArrayList<>();

        for (int n : nums) {

            if (n % 2 == 1) { // BUG
                result.add(n);
            }
        }

        return result;
    }
}

// File: EvenNumberFilterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.*;

class EvenNumberFilterTest {

    @Test
    void testEvenFilter() {
        EvenNumberFilter e =
                new EvenNumberFilter();

        List<Integer> result =
                e.filter(new int[]{1,2,3,4,6});

        assertEquals(3, result.size());
    }

    @Test
    void testSingleEven() {
        EvenNumberFilter e =
                new EvenNumberFilter();

        List<Integer> result =
                e.filter(new int[]{8});

        assertEquals(8, result.get(0));
    }
}

### SAMPLE 1722
// Class: BrokenMultiplier
// Status: COMPILATION_ERROR
// File: BrokenMultiplier.java
public class BrokenMultiplier {

    public int multiply(int a, int b) {
        return a * b
    }
}

// File: BrokenMultiplierTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BrokenMultiplierTest {

    @Test
    void testMultiply() {
        BrokenMultiplier b =
                new BrokenMultiplier();

        assertEquals(12, b.multiply(3,4));
    }
}

### SAMPLE 1723
// Class: CharacterFrequencyCounter
// Status: CORRECT
// File: CharacterFrequencyCounter.java
import java.util.*;

public class CharacterFrequencyCounter {

    public Map<Character,Integer>
    count(String text) {

        Map<Character,Integer> map =
                new HashMap<>();

        for (char c : text.toCharArray()) {

            map.put(
                    c,
                    map.getOrDefault(c,0) + 1
            );
        }

        return map;
    }
}

// File: CharacterFrequencyCounterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.*;

class CharacterFrequencyCounterTest {

    @Test
    void testCount() {
        CharacterFrequencyCounter c =
                new CharacterFrequencyCounter();

        Map<Character,Integer> result =
                c.count("apple");

        assertEquals(2, result.get('p'));
    }

    @Test
    void testSingle() {
        CharacterFrequencyCounter c =
                new CharacterFrequencyCounter();

        Map<Character,Integer> result =
                c.count("a");

        assertEquals(1, result.get('a'));
    }
}

### SAMPLE 1724
// Class: DivisionCrashHandler
// Status: RUNTIME_ERROR
// File: DivisionCrashHandler.java
public class DivisionCrashHandler {

    public int divide(int a, int b) {
        return a / b;
    }
}

// File: DivisionCrashHandlerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DivisionCrashHandlerTest {

    @Test
    void testDivide() {
        DivisionCrashHandler d =
                new DivisionCrashHandler();

        assertEquals(2, d.divide(10,5));
    }

    @Test
    void testCrash() {
        DivisionCrashHandler d =
                new DivisionCrashHandler();

        assertThrows(
                ArithmeticException.class,
                () -> d.divide(5,0)
        );
    }
}

### SAMPLE 1725
// Class: ReverseStringBuilder
// Status: CORRECT
// File: ReverseStringBuilder.java
public class ReverseStringBuilder {

    public String reverse(String text) {

        StringBuilder sb =
                new StringBuilder();

        for (int i = text.length() - 1;
             i >= 0;
             i--) {

            sb.append(text.charAt(i));
        }

        return sb.toString();
    }
}

// File: ReverseStringBuilderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ReverseStringBuilderTest {

    @Test
    void testReverse() {
        ReverseStringBuilder r =
                new ReverseStringBuilder();

        assertEquals(
                "olleh",
                r.reverse("hello")
        );
    }

    @Test
    void testSingle() {
        ReverseStringBuilder r =
                new ReverseStringBuilder();

        assertEquals(
                "a",
                r.reverse("a")
        );
    }
}

### SAMPLE 1726
// Class: WrongAverageCalculator
// Status: BUGGY
// File: WrongAverageCalculator.java
public class WrongAverageCalculator {

    public double average(int[] nums) {

        int sum = 0;

        for (int n : nums) {
            sum += n;
        }

        return sum / (nums.length + 1); // BUG
    }
}

// File: WrongAverageCalculatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class WrongAverageCalculatorTest {

    @Test
    void testAverage() {
        WrongAverageCalculator w =
                new WrongAverageCalculator();

        assertEquals(
                3.0,
                w.average(new int[]{2,3,4})
        );
    }

    @Test
    void testSingle() {
        WrongAverageCalculator w =
                new WrongAverageCalculator();

        assertEquals(
                5.0,
                w.average(new int[]{5})
        );
    }
}

### SAMPLE 1727
// Class: StackPeekManager
// Status: CORRECT
// File: StackPeekManager.java
import java.util.*;

public class StackPeekManager {

    private Stack<Integer> stack =
            new Stack<>();

    public void push(int val) {
        stack.push(val);
    }

    public int peek() {
        return stack.peek();
    }

    public int size() {
        return stack.size();
    }
}

// File: StackPeekManagerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class StackPeekManagerTest {

    @Test
    void testPeek() {
        StackPeekManager s =
                new StackPeekManager();

        s.push(10);
        s.push(20);

        assertEquals(20, s.peek());
    }

    @Test
    void testSize() {
        StackPeekManager s =
                new StackPeekManager();

        s.push(1);

        assertEquals(1, s.size());
    }
}

### SAMPLE 1728
// Class: MissingReturnCase
// Status: COMPILATION_ERROR
// File: MissingReturnCase.java
public class MissingReturnCase {

    public int find(int n) {

        if (n > 0) {
            return n;
        }
    }
}

// File: MissingReturnCaseTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingReturnCaseTest {

    @Test
    void testFind() {
        MissingReturnCase m =
                new MissingReturnCase();

        assertEquals(5, m.find(5));
    }
}

### SAMPLE 1729
// Class: IncorrectPalindromeLength
// Status: BUGGY
// File: IncorrectPalindromeLength.java
public class IncorrectPalindromeLength {

    public boolean check(String text) {

        int left = 0;
        int right = text.length() - 2; // BUG

        while (left < right) {

            if (text.charAt(left) !=
                    text.charAt(right)) {
                return false;
            }

            left++;
            right--;
        }

        return true;
    }
}

// File: IncorrectPalindromeLengthTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectPalindromeLengthTest {

    @Test
    void testPalindrome() {
        IncorrectPalindromeLength i =
                new IncorrectPalindromeLength();

        assertTrue(i.check("madam"));
    }

    @Test
    void testNotPalindrome() {
        IncorrectPalindromeLength i =
                new IncorrectPalindromeLength();

        assertFalse(i.check("hello"));
    }
}

### SAMPLE 1730
// Class: ArrayBoundaryCrash
// Status: RUNTIME_ERROR
// File: ArrayBoundaryCrash.java
public class ArrayBoundaryCrash {

    public int getLast(int[] arr) {
        return arr[arr.length];
    }
}

// File: ArrayBoundaryCrashTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ArrayBoundaryCrashTest {

    @Test
    void testLast() {
        ArrayBoundaryCrash a =
                new ArrayBoundaryCrash();

        assertEquals(
                5,
                a.getLast(new int[]{1,2,5})
        );
    }
}
### SAMPLE 1731
// Class: MaximumValueTracker
// Status: BUGGY
// File: MaximumValueTracker.java
public class MaximumValueTracker {

    public int max(int[] nums) {

        int max = Integer.MAX_VALUE; // BUG

        for (int n : nums) {

            if (n > max) {
                max = n;
            }
        }

        return max;
    }
}

// File: MaximumValueTrackerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MaximumValueTrackerTest {

    @Test
    void testMaximum() {
        MaximumValueTracker m =
                new MaximumValueTracker();

        assertEquals(
                9,
                m.max(new int[]{1,9,4,3})
        );
    }

    @Test
    void testNegative() {
        MaximumValueTracker m =
                new MaximumValueTracker();

        assertEquals(
                -1,
                m.max(new int[]{-5,-1,-8})
        );
    }
}

### SAMPLE 1732
// Class: ConstructorTypoClass
// Status: COMPILATION_ERROR
// File: ConstructorTypoClass.java
public class ConstructorTypoClass {

    public ConstructorTypoClas() {

    }

    public int add(int a, int b) {
        return a + b;
    }
}

// File: ConstructorTypoClassTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ConstructorTypoClassTest {

    @Test
    void testAddition() {
        ConstructorTypoClass c =
                new ConstructorTypoClass();

        assertEquals(9, c.add(4,5));
    }
}

### SAMPLE 1733
// Class: VowelCounterUtility
// Status: CORRECT
// File: VowelCounterUtility.java
public class VowelCounterUtility {

    public int count(String text) {

        int count = 0;

        for (char c : text.toLowerCase().toCharArray()) {

            if ("aeiou".indexOf(c) >= 0) {
                count++;
            }
        }

        return count;
    }
}

// File: VowelCounterUtilityTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class VowelCounterUtilityTest {

    @Test
    void testVowels() {
        VowelCounterUtility v =
                new VowelCounterUtility();

        assertEquals(2, v.count("apple"));
    }

    @Test
    void testNoVowels() {
        VowelCounterUtility v =
                new VowelCounterUtility();

        assertEquals(0, v.count("rhythm"));
    }
}

### SAMPLE 1734
// Class: RecursiveOverflowCrash
// Status: RUNTIME_ERROR
// File: RecursiveOverflowCrash.java
public class RecursiveOverflowCrash {

    public int factorial(int n) {
        return n * factorial(n - 1);
    }
}

// File: RecursiveOverflowCrashTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class RecursiveOverflowCrashTest {

    @Test
    void testFactorial() {
        RecursiveOverflowCrash r =
                new RecursiveOverflowCrash();

        assertEquals(120, r.factorial(5));
    }
}

### SAMPLE 1735
// Class: IncorrectCharacterMatcher
// Status: BUGGY
// File: IncorrectCharacterMatcher.java
public class IncorrectCharacterMatcher {

    public boolean contains(String text,
                            char target) {

        for (char c : text.toCharArray()) {

            if (c == target) {
                return false; // BUG
            }
        }

        return true;
    }
}

// File: IncorrectCharacterMatcherTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectCharacterMatcherTest {

    @Test
    void testContains() {
        IncorrectCharacterMatcher i =
                new IncorrectCharacterMatcher();

        assertTrue(
                i.contains("apple",'p')
        );
    }

    @Test
    void testMissing() {
        IncorrectCharacterMatcher i =
                new IncorrectCharacterMatcher();

        assertFalse(
                i.contains("apple",'z')
        );
    }
}

### SAMPLE 1736
// Class: BinaryConverterTool
// Status: CORRECT
// File: BinaryConverterTool.java
public class BinaryConverterTool {

    public String toBinary(int n) {

        if (n == 0) {
            return "0";
        }

        StringBuilder sb =
                new StringBuilder();

        while (n > 0) {

            sb.append(n % 2);
            n /= 2;
        }

        return sb.reverse().toString();
    }
}

// File: BinaryConverterToolTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BinaryConverterToolTest {

    @Test
    void testBinary() {
        BinaryConverterTool b =
                new BinaryConverterTool();

        assertEquals("1010", b.toBinary(10));
    }

    @Test
    void testZero() {
        BinaryConverterTool b =
                new BinaryConverterTool();

        assertEquals("0", b.toBinary(0));
    }
}

### SAMPLE 1737
// Class: NegativeArrayAllocator
// Status: RUNTIME_ERROR
// File: NegativeArrayAllocator.java
public class NegativeArrayAllocator {

    public int[] create() {
        return new int[-5];
    }
}

// File: NegativeArrayAllocatorTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class NegativeArrayAllocatorTest {

    @Test
    void testArray() {
        NegativeArrayAllocator n =
                new NegativeArrayAllocator();

        assertThrows(
                NegativeArraySizeException.class,
                () -> n.create()
        );
    }
}

### SAMPLE 1738
// Class: PrimeRangeFinder
// Status: CORRECT
// File: PrimeRangeFinder.java
import java.util.*;

public class PrimeRangeFinder {

    public List<Integer> primes(int n) {

        List<Integer> result =
                new ArrayList<>();

        for (int i = 2; i <= n; i++) {

            boolean prime = true;

            for (int j = 2;
                 j * j <= i;
                 j++) {

                if (i % j == 0) {
                    prime = false;
                    break;
                }
            }

            if (prime) {
                result.add(i);
            }
        }

        return result;
    }
}

// File: PrimeRangeFinderTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.*;

class PrimeRangeFinderTest {

    @Test
    void testPrimes() {
        PrimeRangeFinder p =
                new PrimeRangeFinder();

        List<Integer> result =
                p.primes(10);

        assertEquals(4, result.size());
    }

    @Test
    void testSmall() {
        PrimeRangeFinder p =
                new PrimeRangeFinder();

        List<Integer> result =
                p.primes(2);

        assertEquals(2, result.get(0));
    }
}

### SAMPLE 1739
// Class: IncorrectDivisionChecker
// Status: BUGGY
// File: IncorrectDivisionChecker.java
public class IncorrectDivisionChecker {

    public boolean divisible(int a, int b) {
        return a / b == 0; // BUG
    }
}

// File: IncorrectDivisionCheckerTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class IncorrectDivisionCheckerTest {

    @Test
    void testDivisible() {
        IncorrectDivisionChecker i =
                new IncorrectDivisionChecker();

        assertTrue(
                i.divisible(10,5)
        );
    }

    @Test
    void testNotDivisible() {
        IncorrectDivisionChecker i =
                new IncorrectDivisionChecker();

        assertFalse(
                i.divisible(7,3)
        );
    }
}

### SAMPLE 1740
// Class: MissingSemicolonPrinter
// Status: COMPILATION_ERROR
// File: MissingSemicolonPrinter.java
public class MissingSemicolonPrinter {

    public void print() {
        System.out.println("hello")
    }
}

// File: MissingSemicolonPrinterTest.java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MissingSemicolonPrinterTest {

    @Test
    void testPrint() {
        MissingSemicolonPrinter m =
                new MissingSemicolonPrinter();

        m.print();

        assertTrue(true);
    }
}